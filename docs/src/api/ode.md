# ODE coupling

The flat interiors-only state vector, per-block kernels over it, and the
reductions.

```@docs
statelength
statevector
statearray
scatter!
gather!
map_blocks!
block_mapreduce
mesh_mapreduce
volume_weighted_norm
```
