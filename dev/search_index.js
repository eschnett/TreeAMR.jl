var documenterSearchIndex = {"docs":
[{"category":"section","location":"#TreeAMR.jl","page":"Home","text":"TreeAMR.jl implements a tree-based (octree-style) AMR discretization for Julia. It provides the mesh, the storage, and the inter-grid operations — no physics.\n\nSee the design document for the full design and the milestone roadmap.\n\nThe package is at milestone M8: the tree core (Morton keys over a brick of octree roots, neighbor finding, refinement and coarsening, 2:1 balance, periodic wraparound, block storage), the cached ghost exchange with configurable interpolation operators, the state-vector coupling that lets a standard ODE integrator drive the whole hierarchy, adaptive regridding, multi-threading throughout, and GPU support: the storage, the exchange schedule and every kernel follow a KernelAbstractions backend of the caller's choosing.\n\nM8 added the layout and the conservation. The ghost width G is a FieldSet keyword, one per dimension, rather than a Forest one, and a field set also carries a centering — cellcentered, vertexcentered, facecentered or edgecentered — so a GhostSchedule belongs to a layout rather than to a forest. InterfaceSchedule and restrict_interfaces! then make a finite-volume scheme conservative across coarse-fine faces, which under one global dt needs nothing but a spatial flux fixup within each right-hand side.\n\nNext is MPI (M7), so that the distributed exchange is built once over a layout-generic schedule.","title":"TreeAMR.jl"},{"category":"section","location":"#Overview","page":"Home","text":"The domain is a brick of M₁ × … × M_D octree roots. Refinement is all-or-nothing, and only leaves carry data, so the leaves tile the domain exactly:\n\njulia> using TreeAMR\n\njulia> forest = Forest((2, 2); N = 8, periodic = (true, true));\n\njulia> nleaves(forest)\n4\n\njulia> refine!(forest, first(forest.leaves));\n\njulia> nleaves(forest)\n7\n\nThe tree is a linear octree: a sorted flat vector of MortonKeys, with no pointers. Neighbor finding is key arithmetic plus binary search, and periodic wraparound lives in that arithmetic rather than in special boundary code:\n\njulia> k = first(forest.leaves);\n\njulia> neighbor_keys(forest, k, (1, 0))\n1-element Vector{MortonKey{2}}:\n MortonKey{2}(root=0, level=1, coords=(1, 0))\n\nEnforcing balance! guarantees that every ghost region touches at most one level up or down, which is what bounds the ghost-filling cases in M2.\n\nData lives in a FieldSet: one big array over all leaf blocks, cell indices fastest, ghosts included. The ghost width G belongs to the field set rather than to the forest, per dimension: it says how far a stencil reaches into a neighbor's data, which is a property of what is stored. An evolved state with G = 2 and the fluxes computed from it with G = 0, over one forest, is the normal case.\n\njulia> state = FieldSet(forest, 2; G = 2);\n\njulia> size(state.work)\n(12, 12, 2, 7)","title":"Overview"},{"category":"section","location":"#Centerings","page":"Home","text":"A field set also carries a centering: per dimension, its values sit either at cell centers (:cell) or at cell boundaries (:vertex). The familiar names are spellings of that tuple — cellcentered, vertexcentered, facecentered, edgecentered — because every transfer is a product of D one-dimensional stencils, and the stencil for dimension d depends on the centering in that dimension alone.\n\nA vertex-like dimension stores one plane more: the boundary plane a block shares with its high-side neighbor. Ownership is half-open — a block owns its points 0 … N−1 in every dimension, whatever the centering — so the state vector still holds N^D values per block per variable, and the shared plane is filled by the exchange exactly as a ghost is. A flux therefore needs no ghosts at all, only that one extra plane:\n\njulia> flux = FieldSet(forest, 2; G = (0, 0), centering = facecentered(2, 1));\n\njulia> size(flux.work)\n(9, 8, 2, 7)\n\ninteriorview returns the owned points and closedview the owned points plus the shared plane — a block's N+1 faces in the staggered dimension, which is what a flux kernel launched with map_blocks!(...; closed = true) writes. A third launch range, map_blocks!(...; stored = true), covers every stored point, ghosts included; there the kernel's global index is the stored index, while the other two hand out an offset into the owned range that the kernel shifts by G.\n\nIn a vertex-like dimension the inter-grid operators change with it: restriction is exact injection (the coarse point at position X coincides with fine point 2X), and prolongation interpolates at integer or half-integer coarse coordinates, needing G[d] ≥ p/2 − 1 rather than p/2. The conservative family is refused along a stagger, where a staggered quantity stores a point value and there is nothing to conserve.","title":"Centerings"},{"category":"section","location":"#Element-types","page":"Home","text":"The mesh is generic in its floating-point type, and not merely in what it stores: the geometry is computed in that type too, so nothing on the path from a key to a cell center evaluates in Float64 unless that is the type you asked for. This matters because hardware fp64 is not available everywhere — on many GPUs it is absent or an order of magnitude slower.\n\nA Forest carries the type, and FieldSet and GhostSchedule take theirs from it unless told otherwise:\n\njulia> floattype(forest)\nFloat64\n\njulia> small = Forest{Float32}((2, 2); N = 8);\n\njulia> eltype(FieldSet(small, 1; G = 2).work)\nFloat32\n\nInterpolation weights are built in exact rational arithmetic and rounded once, at the point where they enter a stencil, so they carry no error beyond the target type's own — and the conservative family's \"children average back to their parent\" is an algebraic identity rather than a statement about roundoff. Float64, Float32, and a double-Float32 (MultiFloats.jl's Float32x2, which has no hardware support at all) are exercised in the test suite.","title":"Element types"},{"category":"section","location":"#Ghost-exchange","page":"Home","text":"Ghost filling runs at every RHS evaluation, while neighbor finding is only needed when the tree changes. So the exchange is split in two: a GhostSchedule is built once and fill_ghosts! merely replays it, with no tree query in the per-evaluation path.\n\nA schedule belongs to a layout — one ghost width, one centering, one element type, one backend — so it is built from a field set:\n\njulia> schedule = GhostSchedule(state, Operators(prolongation=2, restriction=2));\n\njulia> fill_by_coordinates!((x, v) -> v * x[1], state);\n\njulia> fill_ghosts!(state, schedule);\n\nA coordinate callback is called once per point and variable. Wrapping it in AllVariables selects the once-per-point form instead, which returns every variable at once — what a state that is only definable as a whole needs, such as a set of conserved variables built from a primitive one:\n\njulia> fill_by_coordinates!(AllVariables(x -> (x[1], 2 * x[1])), state);\n\nThe two forms write bit-for-bit the same numbers, and CellBoundary, boundary_by_coordinates and adapt_to_initial_data!'s initial take the wrapper too.\n\nUnder 2:1 balance there are only three cases — a same-level copy, a restriction from finer neighbors, and a prolongation from a coarser one — and all three are tensor products of one-dimensional stencils, so a single KernelAbstractions kernel serves them all.\n\nThe schedule is tied to the tree it was built from, and says so:\n\njulia> isstale(schedule)\nfalse\n\njulia> refine!(forest, last(forest.leaves));\n\njulia> isstale(schedule)\ntrue\n\nInterpolation order is configurable via Operators, and is constrained by the block geometry, per dimension and per centering: order p prolongation needs G[d] ≥ p/2 in a cell-centered dimension and G[d] ≥ p/2 − 1 in a vertex-like one, which check_operators enforces when the schedule is built. It is also constrained by your discretization — see the warning in Operators, which is worth reading before picking an order.","title":"Ghost exchange"},{"category":"section","location":"#Conservation-at-coarse-fine-faces","page":"Home","text":"With one global timestep, a conservative scheme needs only that the flux a coarse cell sees on a coarse-fine face equals the area-weighted sum of the fine-face fluxes there. That is a purely spatial condition, settled within a single right-hand side, so there are no flux registers and no time-accumulated corrections — they exist only to bridge subcycled steps.\n\nA conservative RHS is therefore three steps: compute the fluxes over each face set's closed range, restrict them at coarse-fine faces, then apply the divergence.\n\nmap_blocks!(flux_kernel!, flux, ...; closed = true)   # N+1 faces per dimension\nrestrict_interfaces!(flux, isched)                    # the fixup\nmap_blocks!(divergence_kernel!, state, ...)\n\nInterfaceSchedule is built once per tree like a GhostSchedule, and takes no Operators: the transfer is injection and the exact two-cell average, fixed by the geometry, not interpolation. It is built over the flux field set — the one whose centering is vertex-like in the face dimension — and refuses a cell-centered one, which has no values lying on a block's face at all. It reads and writes closed-range values only, so a computed flux may carry G = 0.","title":"Conservation at coarse-fine faces"},{"category":"section","location":"#Time-integration","page":"Home","text":"The whole hierarchy advances with one global dt, and the state is a single flat vector holding leaf interiors only, so a standard integrator drives it unmodified. Ghosts live in the working array, which is scratch, refreshed at every evaluation.\n\nThe application writes its own f!, calling the three steps explicitly rather than through a semidiscretize-style wrapper:\n\nfunction rhs!(du, u, p, t)\n    scatter!(p.fs, u)                    # flat vector -> working array\n    fill_ghosts!(p.fs, p.schedule)       # copies, restrictions, prolongations\n    map_blocks!(my_kernel!, p.fs, statearray(du, p.fs), p.fs.work, ...)\nend\n\ndt = cfl * minimum_spacing(forest)       # global step, set by the finest level\nsolve(ODEProblem(rhs!, u, tspan, p), RK4(); dt = dt, adaptive = false)\n\nBecause the integrator owns the stages, ghosts are refilled at every evaluation; the classic wide-ghost optimization is unavailable by construction, which CODE.md accepts.\n\nErrors and tolerances on an adaptive mesh want volume_weighted_norm: refined regions contribute more entries per unit volume, so an unweighted norm silently emphasizes them.","title":"Time integration"},{"category":"section","location":"#Regridding","page":"Home","text":"The application flags blocks; regrid! completes the marks to preserve 2:1 balance, rebuilds the key list, and moves the data — surviving blocks copied, refined blocks prolongated from their parent, coarsened blocks restricted from their children.\n\nflags = flag_blocks((b, key) -> needs_refining(fs, b) ? Refine : Keep, forest)\nif regrid!(forest, fs => schedule; flags = flags)\n    schedule = GhostSchedule(fs, operators)       # the old one is now stale\n    u = statevector(fs); gather!(u, fs)           # and u changed length\n    # ... then reinit! the integrator\nend\n\nRegridding changes both the size and the meaning of the state vector, so in practice it means stop → rebuild → reinit! for anything beyond a one-step method. Block indices are not stable across a regrid: slots are compacted, and forest.leaves[b] is the only way to say which block is which.\n\nBuilding initial data iterates the same machinery, re-evaluating the data on each new mesh rather than interpolating it — otherwise a newly refined block would only ever carry the coarse mesh's resolution:\n\nschedule, passes, converged = adapt_to_initial_data!(fs, operators;\n                                                     initial = f, flag = flag)\n\nWhether the transfer conserves total_mass depends on which operator family the field set uses. With Conservative operators it is exact for arbitrary data; with PointValue ones only for fields the operators reproduce exactly, since prolongation is not locally conservative. Coarsening alone conserves either way.\n\nops = Operators(prolongation = 3, restriction = 2, family = Conservative)","title":"Regridding"},{"category":"section","location":"#Threading","page":"Home","text":"Start Julia with threads and everything in the package uses them:\n\njulia -t auto --project=. my_run.jl\n\nThere is no switch to throw and nothing to configure. Per-cell work is KernelAbstractions kernels, whose CPU backend spreads a launch over the available threads, and the host-side passes over blocks — neighbor finding when a GhostSchedule is built, the mark arithmetic in regrid!, the boundary hook, the reductions — are parallel loops over blocks.\n\nResults are bit-identical whatever the thread count, for everything that is not a floating-point sum: every parallel loop writes to its own slot, so the state, the mesh, the schedule and every max or integer reduction on 64 threads reproduce a run on one exactly. A floating-point sum — a norm, a total mass — is promised to roundoff only, so that a device may reduce hierarchically and MPI may Allreduce; today's CPU fold is per-block and so still exact. That is worth relying on when debugging: a difference between two runs beyond the last bits of a sum is never the thread count.\n\nTwo consequences for application code:\n\ncallbacks run concurrently. The f(x, v) of fill_by_coordinates!, the f(b, key) of flag_blocks, and the boundary hook of fill_ghosts! are each called from several threads at once, so they must be pure functions of their arguments (the boundary hook may write the region it was handed, and nothing else);\na block is the unit of parallelism, so a mesh wants appreciably more blocks than threads — a few dozen blocks per thread is comfortable, a handful is not.\n\nOn a multi-socket machine, interleave the pages:\n\nnumactl --interleave=all julia -t 64 --project=. my_run.jl\n\nThis is worth 2–6× at high thread counts and is not something the library can do for you — it is a policy for the whole process. The reason it helps rather than first-touch placement is that the same arrays are partitioned differently by different kernels (the working array by stored cell, the state vector by interior cell, a ghost region by target slab), so no single first-touch pattern serves them all. Measured numbers are in CODE.md; bench/scan.sh reproduces them.","title":"Threading"},{"category":"section","location":"#Devices","page":"Home","text":"The storage decides where the work runs. Pass a KernelAbstractions backend when you allocate, and every kernel in the package follows:\n\nusing CUDA                                # or Metal, or any KA backend\n\nforest   = Forest((4, 4); N = 32, periodic = (true, true),\n                  extents = ((0f0, 1f0), (0f0, 1f0)))\nfs       = FieldSet{Float32}(forest, 2; G = 2, backend = CUDABackend())\nschedule = GhostSchedule(fs, ops)\n\nThere is nothing else to choose. statevector allocates where the field set lives, regrid! reallocates there, and each kernel takes its backend from the storage it is handed — so an RHS written for the CPU is already the device RHS.\n\nThe schedule takes the backend too, and must be built for the same one: its stencil weights and index vectors are read inside the transfer kernel, so they have to live where that kernel runs. They are still built on the host in exact rational arithmetic and uploaded once, when the schedule is built, which is the same argument that put the exchange in a cached schedule in the first place. A mismatch is reported rather than left to fail in memory.\n\nPrecision. The mesh is generic in its floating-point type and computes the geometry and the interpolation weights in it from the first operation, so Float32 needs no fp64 anywhere. A Float64 field set on a backend without hardware fp64 is refused at construction, with the reason.\n\nTwo callbacks change shape on a device, because both used to be host loops over block data:\n\nBoundary conditions. CellBoundary expresses the hook per cell — g(x, v, δ), or g(x, δ) returning every variable at once under AllVariables — and the package launches it as a kernel. boundary_by_coordinates is one of these, so it runs anywhere. The older whole-region form, boundary(fs, b, key, δ, region), is still accepted and is still what a condition that reads the block's interior (reflecting, extrapolating) needs — but it indexes the working array cell by cell, so it is CPU-only, and says so if handed a device field set.\nRefinement criteria. flag_blocks calls f(b, key) on the host, which cannot read device data. firing_boxes is the device form: it evaluates a per-cell predicate over every block in one kernel and returns each block's firing-cell count and bounding box. The verdict — which flag, against which maximum level — stays with the application, because that is physics the mesh cannot know:\n\nfires(work, idx, b, x) = abs(work[idx..., 1, b]) > threshold\n\nflags = map(enumerate(firing_boxes(fires, fs))) do (b, (n, box))\n    n == 0 && return Coarsen\n    level(forest.leaves[b]) < lmax ? (Refine, box) : (Keep, box)\nend\nregrid!(forest, fs => schedule; flags = flags, buffer = 4)\n\nThe box is exactly what regrid!'s buffering dilates, so the device path feeds the same machinery the host one does. adapt_to_initial_data! takes such a criterion through its flags keyword.\n\nbench/gpu.jl times the per-evaluation phases on a chosen backend, in the format bench/threads.jl prints, so a device run and a host run can be read side by side.","title":"Devices"},{"category":"section","location":"#Module","page":"Home","text":"","title":"Module"},{"category":"section","location":"#Tree-core","page":"Home","text":"","title":"Tree core"},{"category":"section","location":"#Forest","page":"Home","text":"","title":"Forest"},{"category":"section","location":"#Geometry","page":"Home","text":"","title":"Geometry"},{"category":"section","location":"#Storage","page":"Home","text":"","title":"Storage"},{"category":"section","location":"#Ghost-exchange-and-operators","page":"Home","text":"","title":"Ghost exchange and operators"},{"category":"section","location":"#Conservation-at-coarse-fine-faces-2","page":"Home","text":"","title":"Conservation at coarse-fine faces"},{"category":"section","location":"#ODE-coupling","page":"Home","text":"","title":"ODE coupling"},{"category":"section","location":"#Regridding-2","page":"Home","text":"","title":"Regridding"},{"category":"section","location":"#Internals","page":"Home","text":"Not exported, and not part of the public interface, but documented because they define the shape of the schedule.\n\nThe host-side threading primitives, for the same reason — the shape of every parallel pass over blocks in the package:\n\nThe device-residency helpers behind the backend keyword:","title":"Internals"},{"category":"section","location":"#Index","page":"Home","text":"","title":"Index"},{"category":"module","location":"#TreeAMR","page":"Home","text":"TreeAMR\n\nA tree-based (octree-style) AMR discretization for Julia. Provides the mesh, the storage, and the inter-grid operations for block-structured adaptive mesh refinement — no physics.\n\nSee CODE.md in the package root for the full design document.\n\n\n\n\n\n","title":"TreeAMR"},{"category":"type","location":"#TreeAMR.MortonKey","page":"Home","text":"MortonKey{D}\n\nA key identifying one node of the forest: the root octree it belongs to, its refinement level, and its per-dimension integer coordinates at that level (each in 0:2^level-1).\n\nKeys are totally ordered by isless: root index first, then Morton (Z-order) curve order, with an ancestor sorting before all of its descendants. The bit interleaving is computed on the fly from level and coords — there is no packed-integer representation, so the depth limit is MAX_LEVEL rather than 64 ÷ D.\n\nDimension 1 is the most significant dimension of the interleaving.\n\n\n\n\n\n","title":"TreeAMR.MortonKey"},{"category":"method","location":"#Base.isless-Union{Tuple{D}, Tuple{MortonKey{D}, MortonKey{D}}} where D","page":"Home","text":"isless(a::MortonKey{D}, b::MortonKey{D})\n\nCurve order: by root index, then by Morton (Z-order) position, with an ancestor immediately preceding its descendants.\n\nRather than walking all MAX_LEVEL bit planes, this finds the dimension whose coordinates first differ at the highest bit and compares only that one — D cheap operations instead of 32D.\n\n\n\n\n\n","title":"Base.isless"},{"category":"constant","location":"#TreeAMR.MAX_LEVEL","page":"Home","text":"MAX_LEVEL\n\nThe deepest refinement level a MortonKey can represent. Bounded by the UInt32 coordinate storage: a coordinate at level L runs over 0:2^L-1, so L cannot exceed 32.\n\n\n\n\n\n","title":"TreeAMR.MAX_LEVEL"},{"category":"function","location":"#TreeAMR.level","page":"Home","text":"level(k::MortonKey)\n\nThe refinement level of k, as an Int.\n\n\n\n\n\n","title":"TreeAMR.level"},{"category":"function","location":"#TreeAMR.parentkey","page":"Home","text":"parentkey(k::MortonKey)\n\nThe key of the node that k is one of the 2^D children of. Errors if k is at root level.\n\n\n\n\n\n","title":"TreeAMR.parentkey"},{"category":"function","location":"#TreeAMR.childkeys","page":"Home","text":"childkeys(k::MortonKey)\n\nThe 2^D keys k splits into under refinement. The order within the tuple is unspecified; use sortedchildkeys when curve order matters.\n\n\n\n\n\n","title":"TreeAMR.childkeys"},{"category":"function","location":"#TreeAMR.sortedchildkeys","page":"Home","text":"sortedchildkeys(k::MortonKey)\n\nThe 2^D children of k in ascending curve order. Because a node's descendants occupy a contiguous run of the Morton curve, splicing these in place of k keeps a sorted leaf array sorted.\n\n\n\n\n\n","title":"TreeAMR.sortedchildkeys"},{"category":"function","location":"#TreeAMR.isancestor","page":"Home","text":"isancestor(a::MortonKey, b::MortonKey)\n\nWhether a is a strict ancestor of b — that is, b lies inside a and is strictly finer.\n\n\n\n\n\n","title":"TreeAMR.isancestor"},{"category":"type","location":"#TreeAMR.Forest","page":"Home","text":"Forest{D,T}\n\nA D-dimensional brick of roots[1] × ... × roots[D] octree roots covering the rectangular physical domain extents, refined into a leaf-only linear octree.\n\nleaves is the sorted (by MortonKey curve order) flat vector of the leaves that currently tile the domain. Only leaves carry data; they tile the domain exactly, with no overlap.\nRefinement is all-or-nothing: a node is either a leaf or has exactly 2^D children. There is never a partially refined node.\nperiodic[d] selects whether dimension d wraps around the brick. Wraparound lives in the neighbor arithmetic, so periodic ghost filling needs no special-casing later.\nN is the per-block interior size, and it is even: cells are the tree's geometry, so N belongs here. The ghost width G does not — it says how far a stencil reaches into a neighbor's data, which is a property of what is stored, and it belongs to the FieldSet (amended in M8; through M6 it was a forest keyword).\nBlocks are cubes, so extents must match the aspect ratio of roots.\nT is the floating-point type the geometry is computed in, not merely stored in — see floattype and \"Precision\" in CODE.md.\n\nRoot indices are linearized 0-based, dimension 1 fastest, over roots; see root_position and root_index.\n\nForest(roots; N, periodic=all false, extents=one unit per root)\nForest{T}(roots; ...)                      # geometry in `T`\n\nExamples\n\njulia> forest = Forest((2, 2); N = 8, periodic = (true, true));\n\njulia> nleaves(forest)\n4\n\n\n\n\n\n","title":"TreeAMR.Forest"},{"category":"function","location":"#TreeAMR.nleaves","page":"Home","text":"nleaves(forest::Forest)\n\nThe number of leaves currently tiling forest; equivalently the number of blocks a FieldSet over it stores.\n\n\n\n\n\n","title":"TreeAMR.nleaves"},{"category":"function","location":"#TreeAMR.maxlevel","page":"Home","text":"maxlevel(forest::Forest)\n\nThe level of the finest leaf currently in forest.\n\n\n\n\n\n","title":"TreeAMR.maxlevel"},{"category":"function","location":"#TreeAMR.root_position","page":"Home","text":"root_position(roots_or_forest, root::Integer)\n\nThe 0-based D-dimensional brick position of root index root (as carried in a MortonKey); the inverse of root_index.\n\n\n\n\n\n","title":"TreeAMR.root_position"},{"category":"function","location":"#TreeAMR.root_index","page":"Home","text":"root_index(roots_or_forest, pos::NTuple)\n\nThe linearized 0-based root index of 0-based brick position pos; the inverse of root_position.\n\n\n\n\n\n","title":"TreeAMR.root_index"},{"category":"function","location":"#TreeAMR.alldirections","page":"Home","text":"alldirections(::Val{D})\n\nAll 3^D - 1 nonzero direction vectors δ ∈ {-1,0,1}^D. One nonzero component names a face, two an edge, and D a corner — the ghost regions that surround a block.\n\n\n\n\n\n","title":"TreeAMR.alldirections"},{"category":"function","location":"#TreeAMR.find_leaf","page":"Home","text":"find_leaf(forest, key) -> Union{Int,Nothing}\nfind_leaf(forest, root, level, coords) -> Union{Int,Nothing}\n\nThe index into forest.leaves of the given leaf, or nothing if that node is not currently a leaf (because it is refined, or coarsened away, or outside the domain). O(log nleaves).\n\n\n\n\n\n","title":"TreeAMR.find_leaf"},{"category":"function","location":"#TreeAMR.isleaf","page":"Home","text":"isleaf(forest, key)\n\nWhether key is currently a leaf of forest.\n\n\n\n\n\n","title":"TreeAMR.isleaf"},{"category":"function","location":"#TreeAMR.neighbor_keys","page":"Home","text":"neighbor_keys(forest, k::MortonKey, δ::NTuple) -> Vector{MortonKey}\n\nThe leaves abutting k across the face, edge, or corner in direction δ (see alldirections) — that is, the leaves that supply the data for that ghost region of k.\n\nValid on any forest, balanced or not. The result is\n\nempty, at a non-periodic domain boundary;\none key, when the neighbor is at k's level or coarser;\notherwise every leaf touching the shared region, at whatever depth.\n\nUnder 2:1 balance (see balance!) the last case is exactly 2^(D - count(!=(0), δ)) keys, all one level finer.\n\nNote that this is not symmetric under δ -> -δ when levels differ: a coarse neighbor found across k's corner also spans the face beyond it, and reversing the direction from that larger block points elsewhere. Adjacency is still mutually discoverable, just not necessarily across the opposite direction.\n\n\n\n\n\n","title":"TreeAMR.neighbor_keys"},{"category":"function","location":"#TreeAMR.refine!","page":"Home","text":"refine!(forest, keys)\n\nReplace each leaf in keys — a single MortonKey or a collection of them — by its 2^D children. Errors if any key is not currently a leaf, or is already at MAX_LEVEL.\n\nDoes not restore 2:1 balance; follow with balance!.\n\n\n\n\n\n","title":"TreeAMR.refine!"},{"category":"function","location":"#TreeAMR.coarsen!","page":"Home","text":"coarsen!(forest, keys)\n\nReplace the 2^D children of each key in keys by that key itself. Errors unless every child of every key is currently a leaf — coarsening is all-or-nothing, matching refinement.\n\nDoes not restore 2:1 balance; follow with balance!.\n\n\n\n\n\n","title":"TreeAMR.coarsen!"},{"category":"function","location":"#TreeAMR.balance!","page":"Home","text":"balance!(forest)\n\nEnforce 2:1 balance across every face, edge, and corner: refine any leaf that is more than one level coarser than a leaf it touches, repeating until the refinement stops rippling outward.\n\nAfterwards every ghost region of every block touches at most one level up or down, which is what bounds the ghost-filling cases and the prolongation stencils.\n\n\n\n\n\n","title":"TreeAMR.balance!"},{"category":"function","location":"#TreeAMR.isbalanced","page":"Home","text":"isbalanced(forest)\n\nWhether forest satisfies 2:1 balance across all faces, edges, and corners — the postcondition of balance!.\n\n\n\n\n\n","title":"TreeAMR.isbalanced"},{"category":"function","location":"#TreeAMR.generation","page":"Home","text":"generation(forest::Forest)\n\nA counter bumped on every change to the leaf array. Structures derived from the tree record it so they can tell in O(1) whether they are still valid — see GhostSchedule.\n\n\n\n\n\n","title":"TreeAMR.generation"},{"category":"function","location":"#TreeAMR.floattype","page":"Home","text":"floattype(forest::Forest)\n\nThe floating-point type forest's geometry is computed in — what spacing, block_origin and friends return, and the element type a FieldSet or GhostSchedule over this forest takes unless told otherwise.\n\n\n\n\n\n","title":"TreeAMR.floattype"},{"category":"function","location":"#TreeAMR.root_spacing","page":"Home","text":"root_spacing([T], forest)\n\nThe physical width of one cell of a root-level block — the coarsest cell size in the hierarchy. Isotropic by construction.\n\nAs everywhere in this file, the optional leading T is the type the arithmetic is done in; it defaults to floattype(forest).\n\n\n\n\n\n","title":"TreeAMR.root_spacing"},{"category":"function","location":"#TreeAMR.spacing","page":"Home","text":"spacing([T], forest, level::Integer)\nspacing([T], forest, k::MortonKey)\n\nThe physical cell size at the given refinement level, halving per level.\n\n\n\n\n\n","title":"TreeAMR.spacing"},{"category":"function","location":"#TreeAMR.minimum_spacing","page":"Home","text":"minimum_spacing([T], forest)\n\nThe cell size of the finest leaf present. This is what sets the global timestep: with no subcycling, the whole hierarchy advances at the CFL limit of its finest cells.\n\n\n\n\n\n","title":"TreeAMR.minimum_spacing"},{"category":"function","location":"#TreeAMR.block_origin","page":"Home","text":"block_origin([T], forest, k::MortonKey)\n\nThe physical position of the lower corner of block k's interior (the outer corner of its first interior cell, not that cell's center).\n\n\n\n\n\n","title":"TreeAMR.block_origin"},{"category":"function","location":"#TreeAMR.block_extent","page":"Home","text":"block_extent([T], forest, k::MortonKey)\n\nThe (lo, hi) physical extent of block k's interior, per dimension. The leaves' extents tile the domain exactly.\n\n\n\n\n\n","title":"TreeAMR.block_extent"},{"category":"function","location":"#TreeAMR.block_spacings","page":"Home","text":"block_spacings(forest, T=floattype(forest))\n\nThe cell size of every leaf, indexed by block — what a kernel needs to scale a finite-difference stencil, since blocks at different levels have different spacings.\n\n\n\n\n\n","title":"TreeAMR.block_spacings"},{"category":"function","location":"#TreeAMR.block_origins","page":"Home","text":"block_origins(forest, T=floattype(forest))\n\nThe lower corner of every leaf's interior, indexed by block — the companion of block_spacings, and what a kernel needs to turn its cell index into a position without consulting the tree. Interior cell i of block b is centred at origins[b][d] + (i - 1/2) * spacings[b].\n\nTogether the two arrays are the whole geometry a device-side kernel sees: plain isbits arrays indexed by block, no keys and no forest.\n\n\n\n\n\n","title":"TreeAMR.block_origins"},{"category":"type","location":"#TreeAMR.FieldSet","page":"Home","text":"FieldSet{T,D,R,A}\n\nBlock storage for nvars variables over every leaf of a Forest: one big persistent array holding all leaf blocks including their ghosts,\n\nwork :: A   # size (N+2G[1]+c[1], ..., N+2G[D]+c[D], nvars, nblocks)\n\nCell indices vary fastest (so a GPU reads them coalesced), then the variable index, then the block index; blocks are ordered by the forest's Morton key order, so forest.leaves[b] is the key of block b.\n\nG is the ghost width, per dimension, and it lives here rather than on the forest (amended in M8): it says how far a stencil reaches into a neighbor's data, which is a property of what is stored and not of how space is cut up. Two field sets over one forest with different G — an evolved state with G = 2 and the fluxes computed from it with G = 0 — is the normal case. It is required: like the operator orders, it follows from the application's discretization, which the mesh cannot know. Pass a plain integer for the uniform case or an NTuple{D,Integer} for one width per dimension; it is stored as an NTuple{D,Int}.\n\ncentering says, per dimension, whether a value sits at a cell center (:cell) or at a cell boundary (:vertex), and defaults to cellcentered(D). Spell it with cellcentered, vertexcentered, facecentered or edgecentered. A vertex-like dimension stores one plane more — the boundary plane the block shares with its high-side neighbor, which that neighbor owns and the exchange fills exactly as it fills a ghost. Three ranges per dimension have names, with c_d = 1 in a vertex-like dimension and 0 otherwise:\n\nstored index role view\n1 … G low ghosts \nG+1 … G+N owned — what the state vector holds interiorview\nG+1 … G+N+c closed — both boundary planes included closedview\nG+N+1 … N+2G+c high exchange region \n\nEverything outside the owned range is exchange-filled, so the asymmetry is bookkeeping in the target ranges and nothing more. The invariant N ≥ 2G[d] + 2c[d] is checked here, per dimension: a block's high exchange region must be reachable from one ring of neighbors, and a finer neighbor spans only N/2 of this block's cells.\n\nElement type T is generic: Float32 for GPUs, Float64 on a host, or a software type such as a double-Float32 where no hardware fp64 exists. It defaults to the forest's own floattype R, which is also the type the geometry is computed in; the two are separate parameters only so that a field set may deliberately store something narrower than its coordinates.\n\nbackend says where the storage lives, and thereby where every kernel over it runs (M6): each one takes its backend from get_backend(fs.work), so this one keyword is the whole switch. A GhostSchedule used with this field set must be built for the same backend. On a device with no hardware fp64 a Float64 field set is rejected here, with a message, rather than failing later inside a kernel compilation.\n\nA field set is tied to the forest's current leaf array. Block indices are deliberately not stable across regridding (M4), which compacts the block slots and rebuilds the storage.\n\nFieldSet(forest, nvars; G)          # element type = floattype(forest)\nFieldSet{Float32}(forest, nvars; G = (2, 0))\nFieldSet(forest, nvars; G = 0, centering = facecentered(3, 1))\nFieldSet{Float32}(forest, nvars; G = 2, backend = CUDABackend())\n\nExamples\n\njulia> forest = Forest((2,); N = 4);\n\njulia> fs = FieldSet(forest, 3; G = 1);\n\njulia> size(fs.work)\n(6, 3, 2)\n\njulia> FieldSet(Forest((1, 1); N = 8), 1; G = (2, 0)).G\n(2, 0)\n\njulia> size(FieldSet(Forest((1, 1); N = 8), 1;\n                     G = 0, centering = facecentered(2, 1)).work)\n(9, 8, 1, 1)\n\n\n\n\n\n","title":"TreeAMR.FieldSet"},{"category":"function","location":"#TreeAMR.cellcentered","page":"Home","text":"cellcentered(D)\n\nThe all-:cell centering: N^D values per block at the cell centers. This is what every field set was through M6, and what FieldSet defaults to.\n\n\n\n\n\n","title":"TreeAMR.cellcentered"},{"category":"function","location":"#TreeAMR.vertexcentered","page":"Home","text":"vertexcentered(D)\n\nThe all-:vertex centering: values at the integer positions, so a block stores its N owned points per dimension plus the boundary plane it shares with its high-side neighbor.\n\n\n\n\n\n","title":"TreeAMR.vertexcentered"},{"category":"function","location":"#TreeAMR.facecentered","page":"Home","text":"facecentered(D, d)\n\nThe centering of a quantity living on the faces normal to dimension d — :vertex in d, :cell in every other dimension. A flux F_d or a face-centered magnetic field B_d has this centering.\n\n\n\n\n\n","title":"TreeAMR.facecentered"},{"category":"function","location":"#TreeAMR.edgecentered","page":"Home","text":"edgecentered(D, d)\n\nThe centering of a quantity living on the edges running along dimension d — :cell in d, :vertex in every other dimension, the complement of facecentered. A constrained-transport EMF E_d or a vector potential A_d has this centering.\n\n\n\n\n\n","title":"TreeAMR.edgecentered"},{"category":"function","location":"#TreeAMR.staggers","page":"Home","text":"staggers(centering::NTuple{D,Symbol})\nstaggers(fs::FieldSet)\n\nThe centering as the arithmetic sees it: c_d = 1 where dimension d is vertex-like, 0 where it is cell-centered. This is the c_d of CODE.md — the extra stored plane, the offset from a stored index to a position, and the length a closed loop adds.\n\n\n\n\n\n","title":"TreeAMR.staggers"},{"category":"function","location":"#TreeAMR.coordinates","page":"Home","text":"coordinates([S], fs::FieldSet, b::Integer, idx::NTuple{D,Integer})\n\nThe physical position of point idx of block b of fs. idx is 1-based over the stored array, so the owned points are G[d]+1 : G[d]+N and values outside that range name the shared plane and the ghosts, whose positions are still well defined and lie on or outside the block's boundary.\n\nThe position depends on the field set's ghost width and on its centering — origin + (i - G[d] - 1/2)·h in a cell-centered dimension, origin + (i - G[d] - 1)·h in a vertex-like one — which is why this takes a field set and a block index rather than a forest and a key (replacing cell_center, which assumed the forest carried G and that every dimension was cell-centered).\n\nThe optional leading S is the type the arithmetic is done in, as everywhere in geometry.jl; it defaults to the field set's own element type T, which is what fill_by_coordinates! and boundary_by_coordinates hand their callbacks.\n\n\n\n\n\n","title":"TreeAMR.coordinates"},{"category":"function","location":"#TreeAMR.nblocks","page":"Home","text":"nblocks(fs::FieldSet)\n\nThe number of blocks stored — one per leaf of the underlying forest.\n\n\n\n\n\n","title":"TreeAMR.nblocks"},{"category":"function","location":"#TreeAMR.blockkey","page":"Home","text":"blockkey(fs::FieldSet, b::Integer)\n\nThe MortonKey of block b.\n\n\n\n\n\n","title":"TreeAMR.blockkey"},{"category":"function","location":"#TreeAMR.blockview","page":"Home","text":"blockview(fs::FieldSet, b::Integer)\nblockview(fs::FieldSet, b::Integer, v::Integer)\n\nA view of block b including ghosts — shape (N+2G[1]+c[1], ..., N+2G[D]+c[D], nvars), or without the trailing nvars for a single variable v.\n\n\n\n\n\n","title":"TreeAMR.blockview"},{"category":"function","location":"#TreeAMR.interiorview","page":"Home","text":"interiorview(fs::FieldSet, b::Integer)\ninteriorview(fs::FieldSet, b::Integer, v::Integer)\n\nA view of block b's owned points — shape (N, ..., N, nvars), or (N, ..., N) for a single variable v. This is the part that tiles the domain and that the ODE state vector holds, for every centering: in a vertex-like dimension a block owns its points 0 … N-1 and the boundary point N belongs to the neighbor on that side (see closedview).\n\n\n\n\n\n","title":"TreeAMR.interiorview"},{"category":"function","location":"#TreeAMR.closedview","page":"Home","text":"closedview(fs::FieldSet, b::Integer)\nclosedview(fs::FieldSet, b::Integer, v::Integer)\n\nA view of block b's closed range — shape (N+c[1], ..., N+c[D], nvars), or without the trailing nvars for a single variable v: the owned points plus the shared boundary plane at the high end of every vertex-like dimension.\n\nThis is what a quantity defined on a block's faces or edges is computed over — a flux F_d has N+1 faces in d, not N — and it is what map_blocks!(...; closed = true) launches over. In a cell-centered field set it is exactly interiorview.\n\nThe extra plane is not owned: it belongs to the high-side neighbor, and the exchange fills it. A kernel launched over the closed range nevertheless writes it, because a flux is needed on both of a block's faces; making the two sides of a coarse-fine face agree afterwards is the interface restriction's job (see CODE.md, \"Conservation at coarse-fine faces\").\n\n\n\n\n\n","title":"TreeAMR.closedview"},{"category":"function","location":"#TreeAMR.fill_by_coordinates!","page":"Home","text":"fill_by_coordinates!(f, fs::FieldSet)\nfill_by_coordinates!(AllVariables(f), fs::FieldSet)\n\nSet every owned point of every block from the callback f(x, v) -> value, where x is that point's position — a cell center, a face center, a vertex, whatever this field set's centering says (an NTuple{D,T} in its own element type, see coordinates) — and v the variable index. The ghosts and the shared boundary plane are left untouched: they are filled by the ghost exchange (M2).\n\nf is called once per point from a KernelAbstractions kernel, so it runs concurrently across blocks (M5) and must be a pure function of its arguments. The tree is not consulted: the kernel gets the geometry as the two plain per-block arrays block_origins and block_spacings, which is what makes it a device kernel (M6).\n\nWrapping the callback in AllVariables selects the second form: f(x) -> vals is then called once per point and returns all fs.nvars values as a tuple, which is what a state that is only definable as a whole needs. The two forms fill a field set with bit-for-bit the same numbers — the position is formed by the same expression, on the same origin and spacing, in the same order — so switching between them is not a numerical change. The tuple's length is checked once on the host, at the first owned point of block 1, before anything is launched.\n\nnote: Callbacks on a device\nThe callback becomes a kernel argument, so everything it closes over must be isbits. A captured Type is the usual trip: write oftype(x[1], 2) rather than closing over T and calling T(2). The same rule covers captured arrays (pass a device array, or index the one the callback is already given) and any mutable state, which the purity requirement rules out anyway.\n\n\n\n\n\n","title":"TreeAMR.fill_by_coordinates!"},{"category":"type","location":"#TreeAMR.AllVariables","page":"Home","text":"AllVariables(f)\n\nWraps a coordinate callback that is called once per point and returns every variable at once, as an NTuple{nvars}, instead of once per point and variable. It selects that form wherever the package takes such a callback: fill_by_coordinates!(AllVariables(f), fs) with f(x) -> vals, CellBoundary(AllVariables(g)) with g(x, δ) -> vals, boundary_by_coordinates(AllVariables(f)), and adapt_to_initial_data!'s initial. The per-variable forms are unchanged and remain the default.\n\nThe reason is that some states are only definable as a whole (added for the TreeHydro application; see CODE.md, \"Application interface\"). A hydrodynamics code states its initial and boundary data as primitive variables and stores conserved ones, and the conversion between them needs all the primitives of a point together: the energy density is built from the density, the velocity and the pressure. Called once per variable, such a callback would redo the whole conversion nvars times per point and throw all but one number away — at setup for the initial data, and at every right-hand side evaluation for the boundary hook.\n\nIt is a wrapper type rather than an arity test on f because a closure does not advertise its arity reliably, and because the package already spells a callback's form as a type (CellBoundary). A single field, so AllVariables(f) is isbits whenever f is and can be a kernel argument like the bare callback.\n\nThe tuple's length is checked once on the host before the launch, since a kernel cannot report it usefully; the message names both numbers.\n\nfill_by_coordinates!(AllVariables(x -> (sum(x), prod(x))), fs)     # nvars == 2\n\nnote: Callbacks on a device\nThe wrapped callback becomes a kernel argument, so everything it closes over must be isbits — the rule the per-variable forms state too. Return a tuple, not a vector. One consequence is specific to this form: the length check evaluates the callback on the host, so one that closes over a device array — which a kernel argument may legally do, since the backend adapts it on the way in — would scalar-index it there. Such a callback needs the per-variable form.\n\n\n\n\n\n","title":"TreeAMR.AllVariables"},{"category":"method","location":"#KernelAbstractions.get_backend-Tuple{FieldSet}","page":"Home","text":"get_backend(fs::FieldSet)\n\nThe KernelAbstractions backend this field set's storage lives on — the backend every kernel over it is launched with.\n\n\n\n\n\n","title":"KernelAbstractions.get_backend"},{"category":"type","location":"#TreeAMR.Operators","page":"Home","text":"Operators(; prolongation, restriction, family=PointValue)\n\nThe operator family and orders a GhostSchedule is built with. Both orders are required — there is deliberately no default. The order is the number of source cells per dimension, and the operator is exact to degree order - 1.\n\nTwo families ship, differing in what a stored number means:\n\nPointValue (default) — finite-difference semantics, data are samples at cell centers. Both operators are polynomial interpolation evaluated at the target center, and both orders must be even and at least 2 so the stencil is symmetric about the target.\nConservative — finite-volume semantics, data are cell averages. Restriction is the exact volume average, so restriction must be 2 (its stencil is a cell's two children per dimension, and it is exact for any field, not merely to some order). prolongation reconstructs a polynomial over the coarse cell and takes subcell averages of it, and must be odd and at least 1, symmetric about the reconstructed cell. Order 1 is piecewise constant, order 3 the familiar ±1/8 slope.\n\nOnly the conservative family makes the regridding transfer exactly mass-conserving for arbitrary data — see regrid! and total_mass. The point-value family conserves only fields it reproduces exactly.\n\nOperators are configured per field set, not per variable — and from M8 on that is per-variable selection: a field set is the unit of centering, ghost width and operators alike, so conservative operators for a density and point-value ones for a velocity are two field sets over one forest, each with its own schedule.\n\nwarning: Choose the order against your discretization\nFor the point-value family, interpolation order must exceed the application's differencing order by two, for both operators, or the coarse-fine interface caps global convergence. This is why there is no default: the right order follows from the application's discretization, which the mesh cannot know. The +2 is the second-derivative case; a flux divergence takes one derivative, so for the conservative family the order must exceed the scheme's by one — measured below.A ghost filled by an order-p operator carries an O(hᵖ) error. A second-derivative stencil divides it by h², so the truncation error along the interface is O(h^{p-2}) — with p = 2 that is O(1), and it does not shrink under refinement at all.Measured with the M3 wave equation (2nd-order Laplacian, two-level mesh), global L2 convergence comes out asprolongation restriction rate\n2 2 1.0\n4 2 0.9\n2 4 1.0\n4 4 2.0Raising one operator alone does not help: each side of the interface gets its ghosts from a different operator, so whichever stays at order 2 keeps its side first order. The same mesh with no refinement converges at 2.0 with order-2 operators, so this is the interface and not the scheme.Along a vertex-like dimension only the prolongation order enters, because restriction there is injection — a coincident point copied, exact for any data, with no order to raise. The same study on a vertex-centered field set measures (M8a)prolongation restriction rate, D = 1 rate, D = 2\n2 2 0.99 1.01\n2 4 0.99 1.01\n4 2 1.99 1.99\n4 4 1.99 1.99at G = 1, where cell centering needs 2. The rows come in pairs because the two runs are the same computation, bit for bit.For the conservative family under a second-order finite-volume scheme the measured rates are (M8b, Burgers' equation, maximum norm; the restriction order does not appear because conservative restriction is exact)prolongation rate, D = 1 rate, D = 2\n1 1.00 0.84\n3 1.97 1.81\n5 1.97 1.77against 1.93 and 1.76 for the same mesh unrefined, so order 3 is the first that does not degrade the scheme and order 5 buys nothing more. The maximum norm: a flux divergence leaves the interface defect where it is instead of radiating it, so an integral norm converges at the scheme's own rate even at order 1. See CODE.md.Order 2 is the cheapest correct interpolation, not the right choice for a second-order-in-space application — which is what a default of 2 used to hide.\n\nThe two operators behave differently at a coarse-fine interface.\n\nProlongation stays symmetric. Its window may reach into the coarse source block's own ghost layers — p ÷ 2 of them for the point-value family, (p - 1) ÷ 2 for the conservative one, at the fine ghost layer nearest the interface — which is what the level-ordered sweep in GhostSchedule exists to guarantee are already filled.\n\nRestriction shifts. A symmetric window at the coarse ghost layer nearest the interface would have to read fine cells across it, and those are themselves prolongated coarse data — a circularity. So the window is shifted inward instead, by p ÷ 2 - 1 fine cells at that layer. This costs no accuracy: Lagrange interpolation through any p distinct nodes is exact for degree < p, and the shifted window still brackets the target, so it stays interpolation and never becomes extrapolation (which the schedule asserts). Conservative restriction never shifts: its window is exactly a cell's own children.\n\nThe orders are therefore constrained by the block geometry, per dimension against that dimension's ghost width and centering (see check_operators). In a cell-centered dimension:\n\nG ≥ prolongation ÷ 2 (point-value) or (prolongation - 1) ÷ 2 (conservative), so a fine block's ghost stencil fits within its coarse neighbor's interior plus that neighbor's own ghosts;\nN ≥ 2G + restriction ÷ 2 - 1 and N ≥ restriction, so a coarse block's ghost layers can be restricted from fine interior cells alone.\n\nIn a vertex-like dimension neither operator is the same object: restriction is injection (no order, no shifting) and prolongation evaluates at integer or half-integer coarse coordinates, so the whole requirement is G ≥ prolongation ÷ 2 - 1 — and the conservative family is refused there outright.\n\n\n\n\n\n","title":"TreeAMR.Operators"},{"category":"type","location":"#TreeAMR.OperatorFamily","page":"Home","text":"OperatorFamily\n\nWhich semantics the stored numbers carry, and so which pair of inter-grid operators applies:\n\nPointValue — samples at cell centers (finite differences). Both operators are polynomial interpolation at the target center.\nConservative — cell averages (finite volumes). Restriction is the exact volume average; prolongation reconstructs over the coarse cell and takes subcell averages, so it is locally conservative by construction.\n\nSee Operators.\n\n\n\n\n\n","title":"TreeAMR.OperatorFamily"},{"category":"function","location":"#TreeAMR.check_operators","page":"Home","text":"check_operators(fs::FieldSet, ops::Operators)\n\nVerify that the forest's N and the field set's per-dimension G and centering support the requested interpolation orders, throwing an ArgumentError naming the violated invariant otherwise. Called when a GhostSchedule is built.\n\nEvery constraint is per dimension, against that dimension's G[d] (amended in M8, with the ghost width) and that dimension's centering (M8a step 2): a family is a rule giving one-dimensional operators per centering, so what a dimension needs depends on where its values sit.\n\nd family restriction prolongation needs, in d\ncell PointValue shifted Lagrange quarter offsets G ≥ p/2, N ≥ 2G+p/2-1\ncell Conservative 2-cell average subcell averages G ≥ (p-1)/2\nvertex PointValue injection integer / half-integer G ≥ p/2 - 1\nvertex Conservative refused refused —\n\nA cell-centered dimension additionally needs N ≥ restriction, so that the restriction window fits inside a fine block's interior; along a stagger there is no window.\n\nA vertex-like dimension needs less, in both directions: restriction is injection, which carries no order and reads a point the fine block owns, and prolongation reaches only p/2 - 1 planes past the shared plane instead of p/2 past the interface. In particular G_d = 0 is legal there — a block still has its shared plane to exchange — and is exactly the second-order constrained-transport layout for an evolved face field.\n\nThere is no blanket requirement of G_d ≥ 1 anywhere: the table implies it wherever interpolation reads a neighbor, which is every order except conservative prolongation of order 1. Piecewise-constant prolongation reads only the coarse cell containing the fine one and the exact average reads only a cell's own children, so a cell-centered dimension with G_d = 0 is a legal layout for Conservative (1, 2) operators — a ghost-free auxiliary field set carried across regrids, say. Its exchange in that dimension is simply empty.\n\n\n\n\n\n","title":"TreeAMR.check_operators"},{"category":"type","location":"#TreeAMR.GhostSchedule","page":"Home","text":"GhostSchedule{T,D,R}\n\nThe precomputed ghost exchange for one forest, replayed by fill_ghosts!.\n\nThe phasing follows CODE.md:\n\nphase1 holds all same-level copies and all restrictions. Each reads only interior cells of other blocks and writes only ghosts, so the whole phase is race free and order independent.\nphase2 holds the prolongations, grouped by target level and ordered coarsest target first; every transfer in a group has its target at that group's level. The sweep is required because a prolongation stencil may read the coarse source's own ghosts, which may themselves have been prolongated from a still-coarser block — legal under 2:1 balance, where levels l-2, l-1, l can meet.\nboundaries lists the ghost regions facing outside a non-periodic domain, filled by the user hook after the inter-block phases; boundaryplan is the same information batched by region shape and resident on the backend, which is what the cell-wise hook form (CellBoundary) is launched over.\nphase1plan and phase2plans cut each phase into balanced PhaseSlices, so that a phase runs as one parallel loop rather than as a sequence of separate launches.\n\nPeriodic boundaries appear nowhere special here: the tree wraps around, so they are ordinary copies, restrictions, and prolongations.\n\nA schedule is tied to the forest's leaf array as it was when built. It must be rebuilt after any refinement, coarsening, or regridding.\n\nGhostSchedule(fs::FieldSet, operators::Operators)\n\noperators is required: interpolation order follows from the application's discretization, so there is no order the mesh could sensibly default to. See Operators.\n\nA schedule is built for one layout — one ghost width, one centering, one element type, one backend — which is exactly what a field set is, so it takes one (amended in M8, when G moved off the forest). fill_ghosts! refuses a field set whose layout differs from the one the schedule was built for. The forest form GhostSchedule(forest, ops; G, centering, T, backend) spells the four out instead, for a caller with no field set in hand.\n\nbackend must be the backend of every field set the schedule is replayed over (M6). The stencil weights and index vectors are read inside the transfer kernel, so they have to live where it runs; they are built on the host in exact rational arithmetic and uploaded once, here, rather than at every ghost fill. That is the same argument that put the exchange in a cached schedule in the first place, applied one level down.\n\n\n\n\n\n","title":"TreeAMR.GhostSchedule"},{"category":"function","location":"#TreeAMR.isstale","page":"Home","text":"isstale(schedule::GhostSchedule)\nisstale(schedule::InterfaceSchedule)\n\nWhether the forest has changed since schedule was built, in which case it must be rebuilt before fill_ghosts! — or restrict_interfaces! — will accept it.\n\n\n\n\n\n","title":"TreeAMR.isstale"},{"category":"function","location":"#TreeAMR.fill_ghosts!","page":"Home","text":"fill_ghosts!(fs::FieldSet, schedule::GhostSchedule; boundary=nothing)\n\nFill every ghost cell of every block, by replaying schedule.\n\nThe three cases — same-level copy, restriction from finer neighbors, prolongation from a coarser neighbor — run in the phases described in GhostSchedule: copies and restrictions together first, then prolongations swept coarsest target first, then the physical boundary hook. Each phase is one parallel loop with a barrier after it; how the phase is cut up for the threads is an implementation detail of that loop (see PhaseSlice).\n\nPeriodic boundaries need nothing special; the tree wraps around, so they are ordinary transfers.\n\nboundary is called once per ghost region facing outside a non-periodic domain, as\n\nboundary(fs, blockindex, key, δ, region)\n\nwith key the block's MortonKey, δ the outward direction, and region the CartesianIndices of the ghost cells in stored coordinates. Use coordinates to get their positions. Passing nothing leaves those ghosts untouched, which is what a fully periodic domain wants.\n\nIn a vertex-like dimension the outward region on the domain's high side starts at the shared boundary plane G+N+1, one plane further in than a ghost slab: those points belong to nobody, since ownership is half-open, so the hook fills them and the application never evolves them. The low boundary points are owned and evolved as usual. This asymmetry is the price of a uniform N^D state layout for every centering; see \"Centerings\" in CODE.md.\n\nnote: Where the boundary hook runs\nThe hook runs after the copies and restrictions but before the prolongation sweep, not after every inter-block phase. A block sitting against the domain edge has prolongation stencils that reach tangentially past that edge, into the coarse source's own outer ghosts; filling those last would feed unwritten memory into the interpolation. The hook may therefore read the block's interior (as reflecting and extrapolating conditions do) but not other blocks' ghosts.\n\nnote: The hook is called concurrently\nThe boundary regions are a parallel loop like every other phase (M5): the hook runs on several threads at once, once per region. Regions are disjoint, so a hook that writes only the region it was handed needs nothing further; one that accumulates into shared state of its own must synchronize itself.\n\n\n\n\n\n","title":"TreeAMR.fill_ghosts!"},{"category":"function","location":"#TreeAMR.boundary_by_coordinates","page":"Home","text":"boundary_by_coordinates(f)\nboundary_by_coordinates(AllVariables(f))\n\nA boundary hook that sets each outer ghost cell from f(x, v), with x the cell center and v the variable index — the same signature fill_by_coordinates! takes.\n\nUseful when the exact solution is known (manufactured solutions, convergence tests); real applications supply their own hook to impose outgoing, reflecting, or symmetry conditions.\n\nA CellBoundary that ignores the direction, so it runs on every backend. Wrapped in AllVariables it is CellBoundary(AllVariables((x, δ) -> f(x))): the same hook built from a once-per-cell f(x) -> vals, which is the shape of a Dirichlet condition set from initial data that is itself stated all at once.\n\n\n\n\n\n","title":"TreeAMR.boundary_by_coordinates"},{"category":"type","location":"#TreeAMR.CellBoundary","page":"Home","text":"CellBoundary(g)\nCellBoundary(AllVariables(g))\n\nA boundary hook expressed per cell: g(x, v, δ) -> value, with x the cell center, v the variable index, and δ the outward direction of the region the cell belongs to.\n\nThis is the form that runs on a device. The package launches it as a kernel over the outward-facing ghost cells, batched by region shape (see BoundaryBatch), so g must be a pure function of its arguments — it never sees the field set and cannot read the block's interior.\n\nThat is the trade. Conditions defined by position alone — Dirichlet data, a manufactured solution, an analytic exterior — are exactly this shape. Conditions that read the interior (reflecting, extrapolating outflow) are not, and stay with the region form fill_ghosts! also accepts, which is CPU-only.\n\nWrapping g in AllVariables selects the once-per-cell form, g(x, δ) -> vals, which drops the variable index and returns all fs.nvars values as a tuple. That is what a boundary state definable only as a whole needs — a hydrodynamics code's Dirichlet data is a primitive state converted to a conserved one — and it matters more here than for the initial data, because this hook runs at every ghost fill, hence at every right-hand side evaluation. The two forms write bit-for-bit the same numbers. The tuple's length is checked on the host before each launch, at the first owned point of block 1 with the sample direction δ = (-1, 0, …, 0); any outward direction would do, since what is checked is how many values come back.\n\nfill_ghosts!(fs, schedule; boundary = CellBoundary((x, v, δ) -> zero(eltype(x))))\nfill_ghosts!(fs, schedule;                         # nvars == 2\n             boundary = CellBoundary(AllVariables((x, δ) -> (x[1], -x[1]))))\n\nnote: Callbacks on a device\nThe callback becomes a kernel argument, so everything it closes over must be isbits. A captured Type is the usual trip: write oftype(x[1], 2) rather than closing over T and calling T(2). The same rule covers captured arrays (pass a device array, or index the one the callback is already given) and any mutable state, which the purity requirement rules out anyway.\n\n\n\n\n\n","title":"TreeAMR.CellBoundary"},{"category":"type","location":"#TreeAMR.InterfaceSchedule","page":"Home","text":"InterfaceSchedule{T,D,R}\n\nThe precomputed coarse-fine interface restriction for one field set, replayed by restrict_interfaces!.\n\nisched = InterfaceSchedule(flux)        # rebuilt when the tree changes\nrestrict_interfaces!(flux, isched)\n\nA face direction ±e_d is admissible when dimension d is vertex-like, so that the field has points lying on that boundary face; a cell-centered field set has no admissible direction at all and is refused here rather than silently given an empty schedule. Edge directions are deliberately absent: the constrained-transport consistency condition lives on the edges of coarse-fine faces, including their boundary lines, and a finer neighbor across an edge alone imposes nothing (see CODE.md).\n\nFor every block, every admissible direction, and every finer neighbor across it, the block's boundary plane in d — its owned plane G+1 on the low side, its shared plane G+N+1 on the high side — is overwritten with the restriction of that neighbor's opposite boundary plane: injection along vertex-like dimensions, the exact two-cell average along cell-like ones. Tangentially the target is the block's owned range in a cell-like dimension and its closed range in a vertex-like one, split half-open among the finer neighbors.\n\nThere is one phase per face dimension, run in ascending order. Within a phase every target point is written exactly once; a point on the line where two coarse-fine faces of the same block meet is written in two phases, by two same-level fine blocks that computed the same value there.\n\nThe schedule is tied to the forest's leaf array and to the field set's layout as they were when it was built, and restrict_interfaces! refuses a stale or mismatched one — as GhostSchedule does, and for the same reason: every target range in it is wrong for another mesh or another G.\n\nUnlike a GhostSchedule this takes no Operators. The transfer is not interpolation: it is injection and the exact two-cell average, fixed by the geometry, so there is no order to choose.\n\n\n\n\n\n","title":"TreeAMR.InterfaceSchedule"},{"category":"function","location":"#TreeAMR.restrict_interfaces!","page":"Home","text":"restrict_interfaces!(fs::FieldSet, isched::InterfaceSchedule)\n\nOverwrite every coarse-fine boundary plane of fs with the restriction of the finer side, by replaying isched.\n\nThis is step (ii) of a conservative right-hand side — compute fluxes over the closed range, restrict them here, then apply the divergence — and it is what makes the two sides of a coarse-fine face agree on the area-weighted flux, hence what makes the scheme conservative to roundoff under a global timestep. See InterfaceSchedule for what is transferred and CODE.md for why nothing more is needed.\n\nThe phases (one per face dimension) run in ascending order with a barrier between them, exactly as fill_ghosts!'s do. It touches neither ghosts nor interior values away from a coarse-fine face, so the field set it runs over may have G = 0.\n\n\n\n\n\n","title":"TreeAMR.restrict_interfaces!"},{"category":"function","location":"#TreeAMR.statelength","page":"Home","text":"statelength(fs::FieldSet)\n\nNumber of entries in this field set's state vector: N^D · nvars · nblocks, counting interior cells only.\n\n\n\n\n\n","title":"TreeAMR.statelength"},{"category":"function","location":"#TreeAMR.statevector","page":"Home","text":"statevector(fs::FieldSet)\n\nA freshly allocated, zeroed state vector for fs — the u an ODE integrator advances. Use gather! to load the field set's current interior values into it.\n\nAllocated on the field set's own backend, so a device-resident field set gets a device-resident state vector and the integrator never touches host memory (M6).\n\n\n\n\n\n","title":"TreeAMR.statevector"},{"category":"function","location":"#TreeAMR.statearray","page":"Home","text":"statearray(u, fs::FieldSet)\n\nView a flat state vector as an (N, ..., N, nvars, nblocks) array, sharing memory. This is the layout application kernels write du in; the index order matches the working array, minus the ghosts.\n\n\n\n\n\n","title":"TreeAMR.statearray"},{"category":"function","location":"#TreeAMR.scatter!","page":"Home","text":"scatter!(fs::FieldSet, u::AbstractVector)\n\nCopy a state vector into the working array's interior cells, leaving ghosts untouched. The first step of every RHS evaluation; follow it with fill_ghosts!.\n\n\n\n\n\n","title":"TreeAMR.scatter!"},{"category":"function","location":"#TreeAMR.gather!","page":"Home","text":"gather!(u::AbstractVector, fs::FieldSet)\n\nCopy the working array's interior cells into a state vector — the inverse of scatter!.\n\nAn application's RHS kernels normally write du in state layout directly, fusing this away; gather! is for setting up initial data and for reading results back out.\n\n\n\n\n\n","title":"TreeAMR.gather!"},{"category":"function","location":"#TreeAMR.map_blocks!","page":"Home","text":"map_blocks!(kernel!, fs::FieldSet, args...; closed=false, stored=false)\n\nLaunch a KernelAbstractions kernel over every owned point of every block, with ndrange = (N, ..., N, nblocks). The kernel's global index is therefore (i1, ..., iD, b) with each i running over 1:N; add G[d] to reach the working array's stored indices.\n\nWith closed = true the loop runs over the closed range instead — N + c[d] per dimension, so a vertex-like dimension also covers the shared boundary plane (see closedview). That is what a quantity defined on a block's faces wants: a flux has N+1 faces per dimension, not N, and the extra one is the block's own high face. In a cell-centered field set the two are the same loop.\n\nWith stored = true the loop runs over every stored point of every block, ghosts included — ndrange = (size(fs.work)[1:D]..., nblocks(fs)), that is N + 2G[d] + c[d] per dimension.\n\nwarning: The three forms index differently\nUnder stored = true the kernel's global index is the stored index: there is nothing to add. Under the default and under closed = true it is an offset into the owned range and the kernel adds G[d]. A kernel written for one form is wrong under the other — silently so, since both are in bounds — so a kernel meant for the stored form should not add G anywhere.\n\nstored = true together with closed = true is an error: the closed range is a sub-range of the stored one, so asking for both is a contradiction.\n\nThe stored form exists because a pointwise pass sometimes has to cover the ghosts too. The case that asked for it is a hydrodynamics code exchanging conserved variables and recovering primitive ones from them: its reconstruction reads primitives two cells into the neighbours, so the recovery has to have run in the ghost cells as well. Which points a block stores is the mesh's business, not the application's, so the launch says stored = true rather than the application spelling out N + 2G + c for itself (see CODE.md, \"Application interface\").\n\nBlocks are uniform work units, so this is one flat parallel loop. The CPU backend spreads it over Threads.nthreads() as it stands (M5), and the same launch runs on a device in M6; every work item writes its own output cell, so the result does not depend on how the loop was split.\n\n@kernel function rhs!(du, @Const(work), @Const(h), ::Val{D}, ::Val{G}) where {D,G}\n    I = @index(Global, NTuple)\n    b = I[D + 1]\n    c = ntuple(d -> I[d] + G[d], Val(D))       # stored (ghosted) index\n    du[ntuple(d -> I[d], Val(D))..., 1, b] = work[c..., 2, b]\nend\n\nThe same pass over the stored extent, which reaches the ghosts and does not shift:\n\n@kernel function recover!(prim, @Const(cons), ::Val{D}) where {D}\n    I = @index(Global, NTuple)                 # already a stored index\n    b = I[D + 1]\n    c = ntuple(d -> I[d], Val(D))\n    prim[c..., 1, b] = sqrt(cons[c..., 1, b])\nend\n\n\n\n\n\n","title":"TreeAMR.map_blocks!"},{"category":"function","location":"#TreeAMR.block_mapreduce","page":"Home","text":"block_mapreduce(f, op, init, fs::FieldSet; vars=1:fs.nvars)\nblock_mapreduce(f, op, init, fs::FieldSet, u::AbstractVector; vars=1:fs.nvars)\n\nReduce each block's interior cells to one value: f transforms a cell value and op folds the transformed values into an accumulator that starts at init. The result is a host Vector of length nblocks(fs), indexed by block, with element type taken from init.\n\nThe first form reads the working array's interiors, skipping the ghosts; the second reads a state vector, which has no ghosts to skip. vars is an integer or a contiguous range of variable indices.\n\nThis is the read-side counterpart of map_blocks!, and it is the shape every diagnostic here has. Combining the values is left to the caller, because the useful combination usually weights each block by its own geometry first — see total_mass. Each block's value is bit-identical whatever the thread count, since it is computed from that block's cells alone; a floating-point combination of them is promised to roundoff only, whichever way it is written.\n\nThe largest value of each variable, which a refinement criterion needs for its scale:\n\nscales = [maximum(block_mapreduce(abs, max, zero(eltype(fs.work)), fs; vars=v))\n          for v in 1:fs.nvars]\n\nIt runs wherever the data lives: threaded host reductions over per-block views on the CPU, one kernel work item per block on a device. It synchronizes and copies back to the host, so it belongs at diagnostic or regrid frequency — not inside a right-hand side.\n\nThe fold is one specification on both backends, but its association is not: the host's mapreduce may reassociate op where the kernel's sequential loop cannot. The guarantee is per-block values that are bit-identical across thread counts and, for a floating-point op, agree to roundoff across backends — identical across backends is not claimed, and is not true.\n\nnote: Callbacks on a device\nf and op become kernel arguments, so everything they close over must be isbits. A captured Type is the usual trip: write oftype(x, 2) rather than closing over T and calling T(2). The same rule covers captured arrays and any mutable state.\n\n\n\n\n\n","title":"TreeAMR.block_mapreduce"},{"category":"function","location":"#TreeAMR.volume_weighted_norm","page":"Home","text":"volume_weighted_norm(fs::FieldSet, u::AbstractVector; p=2)\n\nThe p-norm of a state vector with each cell weighted by its volume, normalized by the domain volume — so the result is a resolution independent RMS (or, for p = Inf, the plain maximum).\n\nWeighting matters on an adaptive mesh: refined regions contribute more entries per unit volume simply for being refined, so an unweighted norm silently emphasizes them. This is also the shape an adaptive integrator's internalnorm needs; through M3 only fixed-dt integrators are exercised, so it is used here for error measurement.\n\nThreaded over blocks. The value is reproducible to roundoff across thread counts and backends; on the CPU, where the partials are combined in block order, it is exact across thread counts as well.\n\n\n\n\n\n","title":"TreeAMR.volume_weighted_norm"},{"category":"type","location":"#TreeAMR.RegridFlag","page":"Home","text":"RegridFlag\n\nWhat the application wants done with a block: Refine, Coarsen, or Keep it as is.\n\nFlags are requests, not commands. Refinement is completed outward to maintain 2:1 balance, and coarsening happens only where all 2^D siblings ask for it and balance still permits — see complete_marks.\n\n\n\n\n\n","title":"TreeAMR.RegridFlag"},{"category":"function","location":"#TreeAMR.flag_blocks","page":"Home","text":"flag_blocks(f, forest) -> Vector\n\nBuild a flag vector by calling f(b, key) for every leaf, with b the block index and key its MortonKey.\n\nf may return either a bare RegridFlag or a (flag, box) pair, where box::NTuple{D,UnitRange{Int}} is the bounding box of the cells that fired, in that block's own interior indices 1:N. Reporting a box is what makes a block a source of the regrid buffer, so the form carries meaning beyond the flag — see buffered_flags. The two forms may be mixed within one vector.\n\nf is called once per leaf, threaded over blocks (M5), so it must be a pure function of its arguments — reading field data is fine, writing to shared state of its own is not.\n\nA convenience for host-side flagging; an application is free to produce the vector any other way, which is what will let the flagging kernel run on the device in M6 while the completion logic stays on the host.\n\n\n\n\n\n","title":"TreeAMR.flag_blocks"},{"category":"function","location":"#TreeAMR.buffered_flags","page":"Home","text":"buffered_flags(forest, flags, buffer) -> Vector{RegridFlag}\n\nThe flags of flags widened by a buffer-cell margin around every region whose criterion fired — step 2 of the regridding sequence in CODE.md, evaluated in mark space, before any refine or coarsen is applied and before balancing, without touching forest.\n\nThe source of a margin is a box, not a flag. A block is a dilation source iff it explicitly reports a (flag, box) pair with any flag but Coarsen, or is marked with a bare Refine (whose box defaults to the whole interior, the conservative isotropic case). A bare Keep is never a source — else quiescent blocks would recruit their neighbours and coarsening would die globally — and Coarsen is never a source even with a box.\n\nEach source asks for a level L: level + 1 for Refine, level for Keep. Its box is dilated by buffer cells per dimension at the source's own resolution, and every other leaf the dilated box reaches joins the buffer:\n\nit is promoted to Refine if its level is below L — a neighbour already at or above L is fine enough;\nits Coarsen is demoted to Keep if its level is at or below L, which is what suppresses coarsen/refine flicker at the edge of the refined region. An over-fine neighbour, above L, may still coarsen toward it.\n\nA block holding a feature at its target level therefore returns (Keep, box) and holds an equal-level margin that travels with the feature — the proactive margin a Refine-keyed rule cannot give, since a freshly refined block's box hugs the face the feature entered through.\n\nThe dilated box reaches the neighbour in direction δ exactly when it leaves the block along every nonzero component of δ, so a feature near one face recruits that face's neighbour only, while one near a corner recruits the face, edge, and corner neighbours on that side — the conjunction comes out of the box geometry rather than being computed by hand.\n\nRecruitment is a single pass over the original marks: a block pulled into the buffer does not itself recruit further neighbours. buffer is therefore limited to N cells, one block width, so that the dilated box cannot reach past the first ring of neighbours.\n\n\n\n\n\n","title":"TreeAMR.buffered_flags"},{"category":"function","location":"#TreeAMR.complete_marks","page":"Home","text":"complete_marks(forest, flags; buffer=0) -> Vector{MortonKey}\n\nThe sorted leaf array that flags asks for, completed so that the result is still 2:1 balanced.\n\nflags holds one entry per leaf, each either a RegridFlag or a (flag, box) pair as flag_blocks produces. buffer is a margin in cells, applied first: every block that reports a box (or is marked with a bare Refine) pulls the neighbouring leaves its box comes within buffer cells of up to the level it asks for — level + 1 for Refine, level for Keep. See buffered_flags.\n\nRefinement is applied first, then coarsening — but only for sibling groups where all 2^D children are present as leaves and all of them ask to coarsen, since refinement is all-or-nothing. The result is then balanced, which may refine blocks the application did not flag, and may undo a coarsening that balance cannot support.\n\nforest is not modified.\n\n\n\n\n\n","title":"TreeAMR.complete_marks"},{"category":"function","location":"#TreeAMR.regrid!","page":"Home","text":"regrid!(forest, pairs; flags, buffer=0, boundary=nothing, transfer=true)\n\nRefine and coarsen forest as flags asks, and move every field set's data onto the new mesh. Returns true if the mesh changed.\n\nThe steps are those in CODE.md: widen the refinement requests by a buffer-cell margin (buffered_flags), complete the marks to preserve 2:1 balance (complete_marks), build the new sorted key list, allocate fresh block storage, and transfer — surviving blocks copied, newly refined blocks prolongated from their parent, coarsened blocks restricted from their children.\n\nflags holds a RegridFlag or a (flag, box) pair per leaf, as flag_blocks produces; buffer is a width in cells, the application's choice (feature speed × regrid cadence), and defaults to no buffering.\n\npairs is one fs => schedule or a collection of them, each field set over forest and each schedule the current one for that field set (amended in M8: with G on the field set a schedule belongs to a layout, so a bare field set no longer says which schedule moves it). Every set's storage is replaced in place, so references an application already holds stay valid, but block indices do not survive: slots are compacted, and forest.leaves[b] is the only way to say which block is which.\n\nThe target of every transfer is the new block's owned range, whatever the centering: a fresh block's shared boundary plane and its ghosts are left to the next fill_ghosts!, as ghosts always are. A test that inspects a staggered field set after a regrid has to fill ghosts first.\n\nGhosts are filled from each schedule before that set's transfer, because a prolongation stencil reads its parent's ghost layers; pass boundary if the domain has non-periodic faces. Write fs => nothing for a set that should only be resized — a computed quantity such as a flux, which the next right-hand side overwrites anyway, and whose ghost-free layout has no schedule to fill it from. Afterwards every schedule is stale and the state vector has changed length, so an application must rebuild both:\n\nif regrid!(forest, fs => schedule; flags = flags)\n    schedule = GhostSchedule(fs, operators)\n    u = statevector(fs); gather!(u, fs)      # then reinit! the integrator\nend\n\nSet transfer = false to rebuild the mesh and storage without moving data at all — what the initial-data cycle wants, since it re-evaluates the initial data on the new mesh instead (see adapt_to_initial_data!).\n\n\n\n\n\n","title":"TreeAMR.regrid!"},{"category":"function","location":"#TreeAMR.adapt_to_initial_data!","page":"Home","text":"adapt_to_initial_data!(fs, operators; initial, flag, buffer=0, maxpasses=10,\n                       boundary=nothing)\n\nRun the initialization cycle from CODE.md: fill the initial data, flag, regrid, then re-evaluate the initial data on the new mesh rather than interpolating it, and repeat until the hierarchy stops changing.\n\nRe-evaluating is the point: interpolating initial data onto a newly refined block would bake in the coarse mesh's resolution, so the refinement would never buy anything.\n\ninitial is an (x, v) -> value callback as fill_by_coordinates! takes — or an AllVariables(f) with f(x) -> vals, which the cycle simply hands on, and which is what an initial state definable only as a whole needs; buffer is passed on to regrid!. Returns (schedule, passes, converged); converged is false if the hierarchy was still changing when maxpasses ran out.\n\nThe criterion is given exactly one of two ways:\n\nflag, a (b, key) -> RegridFlag (or (b, key) -> (flag, box)) callback as flag_blocks takes — the host form;\nflags, a callable fs -> flagvector producing the whole vector at once. This is what a device-side criterion wants, since it reduces every block in one kernel: flags = fs -> map(..., firing_boxes(fires, fs)). See firing_boxes.\n\nThe schedule is rebuilt from fs itself, so it carries that field set's ghost width, element type and backend — a device-resident field set adapts without anything further.\n\n\n\n\n\n","title":"TreeAMR.adapt_to_initial_data!"},{"category":"function","location":"#TreeAMR.total_mass","page":"Home","text":"total_mass(fs::FieldSet, var=1)\n\nThe volume integral of one variable over the domain — Σ hᴰ u over every interior cell, with each block weighted by its own cell volume.\n\nWhat regrid! does to this depends on the operator family:\n\nWith Conservative operators it is preserved to roundoff for any field. Restriction is the exact volume average, and prolongation reconstructs over the coarse cell preserving its average, so a parent's children always average back to it.\nWith PointValue operators it is preserved only for fields the operators reproduce exactly. Coarsening alone still conserves any field — order-2 restriction is the 2^D average — but refinement does not: prolongation is not locally conservative, and at a refinement boundary the fine region draws on neighbor values through the parent's ghosts without those neighbors giving anything up.\n\n\n\n\n\n","title":"TreeAMR.total_mass"},{"category":"function","location":"#TreeAMR.firing_boxes","page":"Home","text":"firing_boxes(fires, fs::FieldSet) -> Vector{Tuple{Int,NTuple{D,UnitRange{Int}}}}\n\nPer block, how many interior cells satisfied fires and the bounding box of those cells, in that block's own interior indices 1:N.\n\nfires(work, idx, b, x) -> Bool is evaluated at every owned point of every block from a kernel, so it runs on the field set's backend and must be a pure function of its arguments. work is the whole working array, idx the point's stored index (so work[idx..., v, b] reads it and Base.setindex(idx, idx[d] + 1, d) reaches its neighbour — the ghosts are there, so a stencil may cross a block face), b the block index, and x its position for this field set's centering (see coordinates).\n\nThis is the device half of regridding: the mesh does the per-cell sweep and the min/max reduction, and the application turns the result into flags, which is where the physics is. A criterion with a maximum refinement level reads\n\nflags = map(enumerate(firing_boxes(fires, fs))) do (b, (n, box))\n    n == 0 && return Coarsen\n    level(forest.leaves[b]) < lmax ? (Refine, box) : (Keep, box)\nend\n\nand the (flag, box) pairs go straight to regrid!, whose buffering is driven by exactly this box (see buffered_flags). A block where nothing fired gets the empty box 1:0 in every dimension.\n\nflag_blocks remains the host form, for criteria that want the tree rather than the data.\n\nnote: Callbacks on a device\nThe callback becomes a kernel argument, so everything it closes over must be isbits. A captured Type is the usual trip: write oftype(x[1], 2) rather than closing over T and calling T(2). The same rule covers captured arrays (pass a device array, or index the one the callback is already given) and any mutable state, which the purity requirement rules out anyway.\n\n\n\n\n\n","title":"TreeAMR.firing_boxes"},{"category":"type","location":"#TreeAMR.Stencil1D","page":"Home","text":"Stencil1D{T,VI,MW}\n\nOne dimension of a transfer: for each cell of the target's index range, the first source cell and the order weights to apply from there.\n\nsrcstart[i] and weights[:, i] describe target cell targetfirst + i - 1.\n\nsrcstart and weights are read inside the transfer kernel, so their array types are parameters: a schedule built for a device holds them there (M6). They are always constructed on the host first — the weights are exact rational arithmetic, which is precisely the kind of work a device should not be asked to do — and uploaded once, when the schedule is built. targetfirst is a plain Int passed by value and stays on the host.\n\n\n\n\n\n","title":"TreeAMR.Stencil1D"},{"category":"type","location":"#TreeAMR.TransferGroup","page":"Home","text":"TransferGroup{T,D}\n\nAll transfers that share one set of 1D stencils — same kind, same direction, same child offset, and for prolongations the same target level — reduced to a list of (targetblock, sourceblock) index pairs.\n\nA group is a batch of work, not a unit of scheduling: a phase runs as one parallel loop over PhaseSlices, and a big group is launched in several of them.\n\n\n\n\n\n","title":"TreeAMR.TransferGroup"},{"category":"type","location":"#TreeAMR.BoundaryRegion","page":"Home","text":"BoundaryRegion{D}\n\nA ghost region of a block that faces outside a non-periodic domain, and so is filled by the user's boundary hook rather than by an inter-block transfer.\n\n\n\n\n\n","title":"TreeAMR.BoundaryRegion"},{"category":"type","location":"#TreeAMR.BoundaryBatch","page":"Home","text":"BoundaryBatch{D}\n\nAll outward-facing ghost regions of one shape, reduced to per-region arrays — the boundary counterpart of a TransferGroup, and for the same reason.\n\nA region's extent is G along every nonzero component of δ and N along every zero one, so there are only a handful of distinct shapes however large the domain is. Batching by shape gives every batch a uniform ndrange, which is what lets the cell-wise boundary form (CellBoundary) run as a few kernel launches instead of one per region.\n\nThe three arrays live wherever the schedule's backend is (M6).\n\n\n\n\n\n","title":"TreeAMR.BoundaryBatch"},{"category":"type","location":"#TreeAMR.BoundaryPlan","page":"Home","text":"BoundaryPlan{D}\n\nEverything the cell-wise boundary kernel reads that is not field data: the BoundaryBatches, and the per-block origin and spacing it turns a stored index into a position with.\n\nThe geometry is here rather than recomputed per fill for the same reason the exchange itself is precomputed: it depends only on the tree, which is exactly what a schedule is rebuilt for.\n\n\n\n\n\n","title":"TreeAMR.BoundaryPlan"},{"category":"type","location":"#TreeAMR.PhaseSlice","page":"Home","text":"PhaseSlice\n\nA contiguous run of one TransferGroup's transfers — the unit of work one thread takes when a whole phase is run as a single parallel loop, and the reason cells is carried: the slices are dealt out largest first, so a phase made of one enormous face group and a hundred tiny corner ones still balances.\n\n\n\n\n\n","title":"TreeAMR.PhaseSlice"},{"category":"function","location":"#TreeAMR.lagrange_weights","page":"Home","text":"lagrange_weights(nodes, x)\n\nWeights w with sum(w[i] * u(nodes[i])) == u(x) for every polynomial u of degree less than length(nodes).\n\nExactness holds for any distinct nodes, which is what lets the restriction stencil be shifted away from the coarse-fine interface (to stay inside the fine block's interior) without losing order.\n\nNodes and target are exact rationals, and so is the result: every position a stencil is ever evaluated at is an integer or a quarter integer, so the weights are exact rational numbers and the only rounding in the whole construction is the single conversion into a Stencil1D's element type. Doing this in floating point instead would fix an accuracy ceiling at whatever type the weights were built in — and would need hardware fp64 to reach it.\n\n\n\n\n\n","title":"TreeAMR.lagrange_weights"},{"category":"function","location":"#TreeAMR.unit_lagrange_weights","page":"Home","text":"unit_lagrange_weights(p, ξ)\n\nWeights for a window of p consecutive integer nodes 0:(p-1), evaluated at ξ, memoised. See lagrange_weights for what they mean and why they are exact.\n\n\n\n\n\n","title":"TreeAMR.unit_lagrange_weights"},{"category":"function","location":"#TreeAMR.ghost_layers_read","page":"Home","text":"ghost_layers_read(ops::Operators)\n\nHow many of the coarse source block's own ghost layers a prolongation stencil reaches into — the quantity that ties the operator order to G.\n\n\n\n\n\n","title":"TreeAMR.ghost_layers_read"},{"category":"function","location":"#TreeAMR.threadchunks","page":"Home","text":"threadchunks(n) -> Vector{UnitRange{Int}}\n\n1:n split into one contiguous range per task: as many ranges as there are threads, never more than there are items, and exactly one range when Julia was started single-threaded (so nothing is spawned at all).\n\nThe split is a function of n and Threads.nthreads() alone — never of which thread picks up which range — so anything built from it is reproducible.\n\n\n\n\n\n","title":"TreeAMR.threadchunks"},{"category":"function","location":"#TreeAMR.threaded_foreach","page":"Home","text":"threaded_foreach(f, n)\n\nCall f(i) for every i in 1:n, one task per chunk of threadchunks. On a single thread this is a plain loop — no task is spawned and no closure is boxed — so the serial path pays nothing for the parallel one.\n\nf must write only to storage belonging to its own i: these loops carry no locks and no atomics by design (see the note at the top of threading.jl).\n\n\n\n\n\n","title":"TreeAMR.threaded_foreach"},{"category":"function","location":"#TreeAMR.threaded_chunks","page":"Home","text":"threaded_chunks(f, n)\n\nCall f(c, range) once per chunk of threadchunks, each on its own task, with c the chunk index. A caller that has to collect results rather than write them in place gives each task the slot c of a per-chunk buffer and concatenates the buffers afterwards in chunk order — which is how every collecting pass in this package stays reproducible while allocating one buffer per thread rather than one per block.\n\n\n\n\n\n","title":"TreeAMR.threaded_chunks"},{"category":"function","location":"#TreeAMR.threaded_collect","page":"Home","text":"threaded_collect(f!, T, n) -> Vector{T}\n\nRun f!(buffer, i) for every i in 1:n, where buffer is this task's own Vector{T} to push onto, and return the concatenation of the buffers in index order — the same vector a serial loop pushing onto one buffer would have produced.\n\n\n\n\n\n","title":"TreeAMR.threaded_collect"},{"category":"function","location":"#TreeAMR.todevice","page":"Home","text":"todevice(backend, a::AbstractArray) -> AbstractArray\n\na, in memory the kernels of backend can read.\n\nOn the CPU backend this is a itself: there is nothing to move, and copying would double the footprint of every schedule for nothing. On a device it is a fresh allocation plus one copy.\n\n\n\n\n\n","title":"TreeAMR.todevice"},{"category":"function","location":"#TreeAMR.tohost","page":"Home","text":"tohost(a::AbstractArray) -> Array\n\na as an ordinary host array, without copying when it already is one.\n\nThe mirror of todevice, for the small per-block results a device kernel produces that the host driver logic then has to read: the flag boxes of firing_boxes, the partials of a reduction.\n\n\n\n\n\n","title":"TreeAMR.tohost"}]
}
