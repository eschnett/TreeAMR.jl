var documenterSearchIndex = {"docs":
[{"category":"section","location":"#TreeAMR.jl","page":"Home","text":"TreeAMR.jl implements a tree-based (octree-style) AMR discretization for Julia. It provides the mesh, the storage, and the inter-grid operations — no physics.\n\nSee the design document for the full design and the milestone roadmap.\n\nThe package is at milestone M4: the tree core (Morton keys over a brick of octree roots, neighbor finding, refinement and coarsening, 2:1 balance, periodic wraparound, block storage), the cached ghost exchange with configurable interpolation operators, the state-vector coupling that lets a standard ODE integrator drive the whole hierarchy, and adaptive regridding. Multi-threading arrives in M5.","title":"TreeAMR.jl"},{"category":"section","location":"#Overview","page":"Home","text":"The domain is a brick of M₁ × … × M_D octree roots. Refinement is all-or-nothing, and only leaves carry data, so the leaves tile the domain exactly:\n\njulia> using TreeAMR\n\njulia> forest = Forest((2, 2); N = 8, G = 2, periodic = (true, true));\n\njulia> nleaves(forest)\n4\n\njulia> refine!(forest, first(forest.leaves));\n\njulia> nleaves(forest)\n7\n\nThe tree is a linear octree: a sorted flat vector of MortonKeys, with no pointers. Neighbor finding is key arithmetic plus binary search, and periodic wraparound lives in that arithmetic rather than in special boundary code:\n\njulia> k = first(forest.leaves);\n\njulia> neighbor_keys(forest, k, (1, 0))\n1-element Vector{MortonKey{2}}:\n MortonKey{2}(root=0, level=1, coords=(1, 0))\n\nEnforcing balance! guarantees that every ghost region touches at most one level up or down, which is what bounds the ghost-filling cases in M2.\n\nData lives in a FieldSet: one big array over all leaf blocks, cell indices fastest, ghosts included.","title":"Overview"},{"category":"section","location":"#Ghost-exchange","page":"Home","text":"Ghost filling runs at every RHS evaluation, while neighbor finding is only needed when the tree changes. So the exchange is split in two: a GhostSchedule is built once and fill_ghosts! merely replays it, with no tree query in the per-evaluation path.\n\njulia> schedule = GhostSchedule(forest, Operators(prolongation=2, restriction=2));\n\njulia> state = FieldSet(forest, 2);\n\njulia> fill_by_coordinates!((x, v) -> v * x[1], state);\n\njulia> fill_ghosts!(state, schedule);\n\nUnder 2:1 balance there are only three cases — a same-level copy, a restriction from finer neighbors, and a prolongation from a coarser one — and all three are tensor products of one-dimensional stencils, so a single KernelAbstractions kernel serves them all.\n\nThe schedule is tied to the tree it was built from, and says so:\n\njulia> isstale(schedule)\nfalse\n\njulia> refine!(forest, last(forest.leaves));\n\njulia> isstale(schedule)\ntrue\n\nInterpolation order is configurable via Operators, and is constrained by the block geometry: order p prolongation needs G ≥ p/2, which check_operators enforces when the schedule is built. It is also constrained by your discretization — see the warning in Operators, which is worth reading before picking an order.","title":"Ghost exchange"},{"category":"section","location":"#Time-integration","page":"Home","text":"The whole hierarchy advances with one global dt, and the state is a single flat vector holding leaf interiors only, so a standard integrator drives it unmodified. Ghosts live in the working array, which is scratch, refreshed at every evaluation.\n\nThe application writes its own f!, calling the three steps explicitly rather than through a semidiscretize-style wrapper:\n\nfunction rhs!(du, u, p, t)\n    scatter!(p.fs, u)                    # flat vector -> working array\n    fill_ghosts!(p.fs, p.schedule)       # copies, restrictions, prolongations\n    map_blocks!(my_kernel!, p.fs, statearray(du, p.fs), p.fs.work, ...)\nend\n\ndt = cfl * minimum_spacing(forest)       # global step, set by the finest level\nsolve(ODEProblem(rhs!, u, tspan, p), RK4(); dt = dt, adaptive = false)\n\nBecause the integrator owns the stages, ghosts are refilled at every evaluation; the classic wide-ghost optimization is unavailable by construction, which CODE.md accepts.\n\nErrors and tolerances on an adaptive mesh want volume_weighted_norm: refined regions contribute more entries per unit volume, so an unweighted norm silently emphasizes them.","title":"Time integration"},{"category":"section","location":"#Regridding","page":"Home","text":"The application flags blocks; regrid! completes the marks to preserve 2:1 balance, rebuilds the key list, and moves the data — surviving blocks copied, refined blocks prolongated from their parent, coarsened blocks restricted from their children.\n\nflags = flag_blocks((b, key) -> needs_refining(fs, b) ? Refine : Keep, forest)\nif regrid!(forest, fs, schedule; flags = flags)\n    schedule = GhostSchedule(forest, operators)   # the old one is now stale\n    u = statevector(fs); gather!(u, fs)           # and u changed length\n    # ... then reinit! the integrator\nend\n\nRegridding changes both the size and the meaning of the state vector, so in practice it means stop → rebuild → reinit! for anything beyond a one-step method. Block indices are not stable across a regrid: slots are compacted, and forest.leaves[b] is the only way to say which block is which.\n\nBuilding initial data iterates the same machinery, re-evaluating the data on each new mesh rather than interpolating it — otherwise a newly refined block would only ever carry the coarse mesh's resolution:\n\nschedule, passes, converged = adapt_to_initial_data!(fs, operators;\n                                                     initial = f, flag = flag)\n\nWhether the transfer conserves total_mass depends on which operator family the field set uses. With Conservative operators it is exact for arbitrary data; with PointValue ones only for fields the operators reproduce exactly, since prolongation is not locally conservative. Coarsening alone conserves either way.\n\nops = Operators(prolongation = 3, restriction = 2, family = Conservative)","title":"Regridding"},{"category":"section","location":"#Module","page":"Home","text":"","title":"Module"},{"category":"section","location":"#Tree-core","page":"Home","text":"","title":"Tree core"},{"category":"section","location":"#Forest","page":"Home","text":"","title":"Forest"},{"category":"section","location":"#Geometry","page":"Home","text":"","title":"Geometry"},{"category":"section","location":"#Storage","page":"Home","text":"","title":"Storage"},{"category":"section","location":"#Ghost-exchange-and-operators","page":"Home","text":"","title":"Ghost exchange and operators"},{"category":"section","location":"#ODE-coupling","page":"Home","text":"","title":"ODE coupling"},{"category":"section","location":"#Regridding-2","page":"Home","text":"","title":"Regridding"},{"category":"section","location":"#Internals","page":"Home","text":"Not exported, and not part of the public interface, but documented because they define the shape of the schedule.","title":"Internals"},{"category":"section","location":"#Index","page":"Home","text":"","title":"Index"},{"category":"module","location":"#TreeAMR","page":"Home","text":"TreeAMR\n\nA tree-based (octree-style) AMR discretization for Julia. Provides the mesh, the storage, and the inter-grid operations for block-structured adaptive mesh refinement — no physics.\n\nSee CODE.md in the package root for the full design document.\n\n\n\n\n\n","title":"TreeAMR"},{"category":"type","location":"#TreeAMR.MortonKey","page":"Home","text":"MortonKey{D}\n\nA key identifying one node of the forest: the root octree it belongs to, its refinement level, and its per-dimension integer coordinates at that level (each in 0:2^level-1).\n\nKeys are totally ordered by isless: root index first, then Morton (Z-order) curve order, with an ancestor sorting before all of its descendants. The bit interleaving is computed on the fly from level and coords — there is no packed-integer representation, so the depth limit is MAX_LEVEL rather than 64 ÷ D.\n\nDimension 1 is the most significant dimension of the interleaving.\n\n\n\n\n\n","title":"TreeAMR.MortonKey"},{"category":"method","location":"#Base.isless-Union{Tuple{D}, Tuple{MortonKey{D}, MortonKey{D}}} where D","page":"Home","text":"isless(a::MortonKey{D}, b::MortonKey{D})\n\nCurve order: by root index, then by Morton (Z-order) position, with an ancestor immediately preceding its descendants.\n\nRather than walking all MAX_LEVEL bit planes, this finds the dimension whose coordinates first differ at the highest bit and compares only that one — D cheap operations instead of 32D.\n\n\n\n\n\n","title":"Base.isless"},{"category":"constant","location":"#TreeAMR.MAX_LEVEL","page":"Home","text":"MAX_LEVEL\n\nThe deepest refinement level a MortonKey can represent. Bounded by the UInt32 coordinate storage: a coordinate at level L runs over 0:2^L-1, so L cannot exceed 32.\n\n\n\n\n\n","title":"TreeAMR.MAX_LEVEL"},{"category":"function","location":"#TreeAMR.level","page":"Home","text":"level(k::MortonKey)\n\nThe refinement level of k, as an Int.\n\n\n\n\n\n","title":"TreeAMR.level"},{"category":"function","location":"#TreeAMR.parentkey","page":"Home","text":"parentkey(k::MortonKey)\n\nThe key of the node that k is one of the 2^D children of. Errors if k is at root level.\n\n\n\n\n\n","title":"TreeAMR.parentkey"},{"category":"function","location":"#TreeAMR.childkeys","page":"Home","text":"childkeys(k::MortonKey)\n\nThe 2^D keys k splits into under refinement. The order within the tuple is unspecified; use sortedchildkeys when curve order matters.\n\n\n\n\n\n","title":"TreeAMR.childkeys"},{"category":"function","location":"#TreeAMR.sortedchildkeys","page":"Home","text":"sortedchildkeys(k::MortonKey)\n\nThe 2^D children of k in ascending curve order. Because a node's descendants occupy a contiguous run of the Morton curve, splicing these in place of k keeps a sorted leaf array sorted.\n\n\n\n\n\n","title":"TreeAMR.sortedchildkeys"},{"category":"function","location":"#TreeAMR.isancestor","page":"Home","text":"isancestor(a::MortonKey, b::MortonKey)\n\nWhether a is a strict ancestor of b — that is, b lies inside a and is strictly finer.\n\n\n\n\n\n","title":"TreeAMR.isancestor"},{"category":"type","location":"#TreeAMR.Forest","page":"Home","text":"Forest{D}\n\nA D-dimensional brick of roots[1] × ... × roots[D] octree roots covering the rectangular physical domain extents, refined into a leaf-only linear octree.\n\nleaves is the sorted (by MortonKey curve order) flat vector of the leaves that currently tile the domain. Only leaves carry data; they tile the domain exactly, with no overlap.\nRefinement is all-or-nothing: a node is either a leaf or has exactly 2^D children. There is never a partially refined node.\nperiodic[d] selects whether dimension d wraps around the brick. Wraparound lives in the neighbor arithmetic, so periodic ghost filling needs no special-casing later.\nN is the per-block interior size and G the ghost width, subject to the invariants in CODE.md: N even and N ≥ 2G.\nBlocks are cubes, so extents must match the aspect ratio of roots.\n\nRoot indices are linearized 0-based, dimension 1 fastest, over roots; see root_position and root_index.\n\nForest(roots; N, G, periodic=all false, extents=one unit per root)\n\nExamples\n\njulia> forest = Forest((2, 2); N = 8, G = 2, periodic = (true, true));\n\njulia> nleaves(forest)\n4\n\n\n\n\n\n","title":"TreeAMR.Forest"},{"category":"function","location":"#TreeAMR.nleaves","page":"Home","text":"nleaves(forest::Forest)\n\nThe number of leaves currently tiling forest; equivalently the number of blocks a FieldSet over it stores.\n\n\n\n\n\n","title":"TreeAMR.nleaves"},{"category":"function","location":"#TreeAMR.maxlevel","page":"Home","text":"maxlevel(forest::Forest)\n\nThe level of the finest leaf currently in forest.\n\n\n\n\n\n","title":"TreeAMR.maxlevel"},{"category":"function","location":"#TreeAMR.root_position","page":"Home","text":"root_position(roots_or_forest, root::Integer)\n\nThe 0-based D-dimensional brick position of root index root (as carried in a MortonKey); the inverse of root_index.\n\n\n\n\n\n","title":"TreeAMR.root_position"},{"category":"function","location":"#TreeAMR.root_index","page":"Home","text":"root_index(roots_or_forest, pos::NTuple)\n\nThe linearized 0-based root index of 0-based brick position pos; the inverse of root_position.\n\n\n\n\n\n","title":"TreeAMR.root_index"},{"category":"function","location":"#TreeAMR.alldirections","page":"Home","text":"alldirections(::Val{D})\n\nAll 3^D - 1 nonzero direction vectors δ ∈ {-1,0,1}^D. One nonzero component names a face, two an edge, and D a corner — the ghost regions that surround a block.\n\n\n\n\n\n","title":"TreeAMR.alldirections"},{"category":"function","location":"#TreeAMR.find_leaf","page":"Home","text":"find_leaf(forest, key) -> Union{Int,Nothing}\nfind_leaf(forest, root, level, coords) -> Union{Int,Nothing}\n\nThe index into forest.leaves of the given leaf, or nothing if that node is not currently a leaf (because it is refined, or coarsened away, or outside the domain). O(log nleaves).\n\n\n\n\n\n","title":"TreeAMR.find_leaf"},{"category":"function","location":"#TreeAMR.isleaf","page":"Home","text":"isleaf(forest, key)\n\nWhether key is currently a leaf of forest.\n\n\n\n\n\n","title":"TreeAMR.isleaf"},{"category":"function","location":"#TreeAMR.neighbor_keys","page":"Home","text":"neighbor_keys(forest, k::MortonKey, δ::NTuple) -> Vector{MortonKey}\n\nThe leaves abutting k across the face, edge, or corner in direction δ (see alldirections) — that is, the leaves that supply the data for that ghost region of k.\n\nValid on any forest, balanced or not. The result is\n\nempty, at a non-periodic domain boundary;\none key, when the neighbor is at k's level or coarser;\notherwise every leaf touching the shared region, at whatever depth.\n\nUnder 2:1 balance (see balance!) the last case is exactly 2^(D - count(!=(0), δ)) keys, all one level finer.\n\nNote that this is not symmetric under δ -> -δ when levels differ: a coarse neighbor found across k's corner also spans the face beyond it, and reversing the direction from that larger block points elsewhere. Adjacency is still mutually discoverable, just not necessarily across the opposite direction.\n\n\n\n\n\n","title":"TreeAMR.neighbor_keys"},{"category":"function","location":"#TreeAMR.refine!","page":"Home","text":"refine!(forest, keys)\n\nReplace each leaf in keys — a single MortonKey or a collection of them — by its 2^D children. Errors if any key is not currently a leaf, or is already at MAX_LEVEL.\n\nDoes not restore 2:1 balance; follow with balance!.\n\n\n\n\n\n","title":"TreeAMR.refine!"},{"category":"function","location":"#TreeAMR.coarsen!","page":"Home","text":"coarsen!(forest, keys)\n\nReplace the 2^D children of each key in keys by that key itself. Errors unless every child of every key is currently a leaf — coarsening is all-or-nothing, matching refinement.\n\nDoes not restore 2:1 balance; follow with balance!.\n\n\n\n\n\n","title":"TreeAMR.coarsen!"},{"category":"function","location":"#TreeAMR.balance!","page":"Home","text":"balance!(forest)\n\nEnforce 2:1 balance across every face, edge, and corner: refine any leaf that is more than one level coarser than a leaf it touches, repeating until the refinement stops rippling outward.\n\nAfterwards every ghost region of every block touches at most one level up or down, which is what bounds the ghost-filling cases and the prolongation stencils.\n\n\n\n\n\n","title":"TreeAMR.balance!"},{"category":"function","location":"#TreeAMR.isbalanced","page":"Home","text":"isbalanced(forest)\n\nWhether forest satisfies 2:1 balance across all faces, edges, and corners — the postcondition of balance!.\n\n\n\n\n\n","title":"TreeAMR.isbalanced"},{"category":"function","location":"#TreeAMR.generation","page":"Home","text":"generation(forest::Forest)\n\nA counter bumped on every change to the leaf array. Structures derived from the tree record it so they can tell in O(1) whether they are still valid — see GhostSchedule.\n\n\n\n\n\n","title":"TreeAMR.generation"},{"category":"function","location":"#TreeAMR.root_spacing","page":"Home","text":"root_spacing(forest)\n\nThe physical width of one cell of a root-level block — the coarsest cell size in the hierarchy. Isotropic by construction.\n\n\n\n\n\n","title":"TreeAMR.root_spacing"},{"category":"function","location":"#TreeAMR.spacing","page":"Home","text":"spacing(forest, level::Integer)\nspacing(forest, k::MortonKey)\n\nThe physical cell size at the given refinement level, halving per level.\n\n\n\n\n\n","title":"TreeAMR.spacing"},{"category":"function","location":"#TreeAMR.minimum_spacing","page":"Home","text":"minimum_spacing(forest)\n\nThe cell size of the finest leaf present. This is what sets the global timestep: with no subcycling, the whole hierarchy advances at the CFL limit of its finest cells.\n\n\n\n\n\n","title":"TreeAMR.minimum_spacing"},{"category":"function","location":"#TreeAMR.block_origin","page":"Home","text":"block_origin(forest, k::MortonKey)\n\nThe physical position of the lower corner of block k's interior (the outer corner of its first interior cell, not that cell's center).\n\n\n\n\n\n","title":"TreeAMR.block_origin"},{"category":"function","location":"#TreeAMR.block_extent","page":"Home","text":"block_extent(forest, k::MortonKey)\n\nThe (lo, hi) physical extent of block k's interior, per dimension. The leaves' extents tile the domain exactly.\n\n\n\n\n\n","title":"TreeAMR.block_extent"},{"category":"function","location":"#TreeAMR.cell_center","page":"Home","text":"cell_center(forest, k::MortonKey, idx::NTuple{D,Integer})\n\nThe physical position of the center of cell idx of block k. idx is 1-based over the stored array, so interior cells are G+1:G+N and values outside that range name ghost cells (whose coordinates are still well defined, and lie outside the block).\n\n\n\n\n\n","title":"TreeAMR.cell_center"},{"category":"function","location":"#TreeAMR.block_spacings","page":"Home","text":"block_spacings(forest, T=Float64)\n\nThe cell size of every leaf, indexed by block — what a kernel needs to scale a finite-difference stencil, since blocks at different levels have different spacings.\n\n\n\n\n\n","title":"TreeAMR.block_spacings"},{"category":"type","location":"#TreeAMR.FieldSet","page":"Home","text":"FieldSet{T,D,A}\n\nBlock storage for nvars variables over every leaf of a Forest: one big persistent array holding all leaf blocks including their ghosts,\n\nwork :: A   # size (N+2G, ..., N+2G, nvars, nblocks)\n\nCell indices vary fastest (so a GPU reads them coalesced), then the variable index, then the block index; blocks are ordered by the forest's Morton key order, so forest.leaves[b] is the key of block b.\n\nElement type T is generic — Float64 by default, Float32 for GPUs.\n\nA field set is tied to the forest's current leaf array. Block indices are deliberately not stable across regridding (M4), which compacts the block slots and rebuilds the storage.\n\nFieldSet(forest, nvars)          # Float64\nFieldSet{Float32}(forest, nvars)\n\nExamples\n\njulia> forest = Forest((2,); N = 4, G = 1);\n\njulia> fs = FieldSet(forest, 3);\n\njulia> size(fs.work)\n(6, 3, 2)\n\n\n\n\n\n","title":"TreeAMR.FieldSet"},{"category":"function","location":"#TreeAMR.nblocks","page":"Home","text":"nblocks(fs::FieldSet)\n\nThe number of blocks stored — one per leaf of the underlying forest.\n\n\n\n\n\n","title":"TreeAMR.nblocks"},{"category":"function","location":"#TreeAMR.blockkey","page":"Home","text":"blockkey(fs::FieldSet, b::Integer)\n\nThe MortonKey of block b.\n\n\n\n\n\n","title":"TreeAMR.blockkey"},{"category":"function","location":"#TreeAMR.blockview","page":"Home","text":"blockview(fs::FieldSet, b::Integer)\nblockview(fs::FieldSet, b::Integer, v::Integer)\n\nA view of block b including ghosts — shape (N+2G, ..., N+2G, nvars), or (N+2G, ..., N+2G) for a single variable v.\n\n\n\n\n\n","title":"TreeAMR.blockview"},{"category":"function","location":"#TreeAMR.interiorview","page":"Home","text":"interiorview(fs::FieldSet, b::Integer)\ninteriorview(fs::FieldSet, b::Integer, v::Integer)\n\nA view of block b excluding ghosts — shape (N, ..., N, nvars), or (N, ..., N) for a single variable v. This is the part that tiles the domain and that the ODE state vector holds.\n\n\n\n\n\n","title":"TreeAMR.interiorview"},{"category":"function","location":"#TreeAMR.fill_by_coordinates!","page":"Home","text":"fill_by_coordinates!(f, fs::FieldSet)\n\nSet every interior cell of every block from the callback f(x, v) -> value, where x is the cell center (an NTuple{D,Float64}, see cell_center) and v the variable index. Ghosts are left untouched — they are filled by the ghost exchange (M2).\n\nSerial and allocation-light; the parallel and GPU versions come with the KernelAbstractions kernels in M2 and M6.\n\n\n\n\n\n","title":"TreeAMR.fill_by_coordinates!"},{"category":"type","location":"#TreeAMR.Operators","page":"Home","text":"Operators(; prolongation, restriction, family=PointValue)\n\nThe operator family and orders a GhostSchedule is built with. Both orders are required — there is deliberately no default. The order is the number of source cells per dimension, and the operator is exact to degree order - 1.\n\nTwo families ship, differing in what a stored number means:\n\nPointValue (default) — finite-difference semantics, data are samples at cell centers. Both operators are polynomial interpolation evaluated at the target center, and both orders must be even and at least 2 so the stencil is symmetric about the target.\nConservative — finite-volume semantics, data are cell averages. Restriction is the exact volume average, so restriction must be 2 (its stencil is a cell's two children per dimension, and it is exact for any field, not merely to some order). prolongation reconstructs a polynomial over the coarse cell and takes subcell averages of it, and must be odd and at least 1, symmetric about the reconstructed cell. Order 1 is piecewise constant, order 3 the familiar ±1/8 slope.\n\nOnly the conservative family makes the regridding transfer exactly mass-conserving for arbitrary data — see regrid! and total_mass. The point-value family conserves only fields it reproduces exactly.\n\nOperators are configured per field set, not per variable — see CODE.md; per-variable selection is deferred to M8.\n\nwarning: Choose the order against your discretization\nFor the point-value family, interpolation order must exceed the application's differencing order by two, for both operators, or the coarse-fine interface caps global convergence. This is why there is no default: the right order follows from the application's discretization, which the mesh cannot know. (How the rule carries over to the conservative family is to be measured in M8.)A ghost filled by an order-p operator carries an O(hᵖ) error. A second-derivative stencil divides it by h², so the truncation error along the interface is O(h^{p-2}) — with p = 2 that is O(1), and it does not shrink under refinement at all.Measured with the M3 wave equation (2nd-order Laplacian, two-level mesh), global L2 convergence comes out asprolongation restriction rate\n2 2 1.0\n4 2 0.9\n2 4 1.0\n4 4 2.0Raising one operator alone does not help: each side of the interface gets its ghosts from a different operator, so whichever stays at order 2 keeps its side first order. The same mesh with no refinement converges at 2.0 with order-2 operators, so this is the interface and not the scheme.Order 2 is the cheapest correct interpolation, not the right choice for a second-order-in-space application — which is what a default of 2 used to hide.\n\nThe two operators behave differently at a coarse-fine interface.\n\nProlongation stays symmetric. Its window may reach into the coarse source block's own ghost layers — p ÷ 2 of them for the point-value family, (p - 1) ÷ 2 for the conservative one, at the fine ghost layer nearest the interface — which is what the level-ordered sweep in GhostSchedule exists to guarantee are already filled.\n\nRestriction shifts. A symmetric window at the coarse ghost layer nearest the interface would have to read fine cells across it, and those are themselves prolongated coarse data — a circularity. So the window is shifted inward instead, by p ÷ 2 - 1 fine cells at that layer. This costs no accuracy: Lagrange interpolation through any p distinct nodes is exact for degree < p, and the shifted window still brackets the target, so it stays interpolation and never becomes extrapolation (which the schedule asserts). Conservative restriction never shifts: its window is exactly a cell's own children.\n\nThe orders are therefore constrained by the block geometry (see check_operators):\n\nG ≥ prolongation ÷ 2 (point-value) or (prolongation - 1) ÷ 2 (conservative), so a fine block's ghost stencil fits within its coarse neighbor's interior plus that neighbor's own ghosts;\nN ≥ 2G + restriction ÷ 2 - 1 and N ≥ restriction, so a coarse block's ghost layers can be restricted from fine interior cells alone.\n\n\n\n\n\n","title":"TreeAMR.Operators"},{"category":"type","location":"#TreeAMR.OperatorFamily","page":"Home","text":"OperatorFamily\n\nWhich semantics the stored numbers carry, and so which pair of inter-grid operators applies:\n\nPointValue — samples at cell centers (finite differences). Both operators are polynomial interpolation at the target center.\nConservative — cell averages (finite volumes). Restriction is the exact volume average; prolongation reconstructs over the coarse cell and takes subcell averages, so it is locally conservative by construction.\n\nSee Operators.\n\n\n\n\n\n","title":"TreeAMR.OperatorFamily"},{"category":"function","location":"#TreeAMR.check_operators","page":"Home","text":"check_operators(forest, ops::Operators)\n\nVerify that forest's N and G support the requested interpolation orders, throwing an ArgumentError naming the violated invariant otherwise. Called when a GhostSchedule is built.\n\n\n\n\n\n","title":"TreeAMR.check_operators"},{"category":"type","location":"#TreeAMR.GhostSchedule","page":"Home","text":"GhostSchedule{T,D}\n\nThe precomputed ghost exchange for one forest, replayed by fill_ghosts!.\n\nThe phasing follows CODE.md:\n\nphase1 holds all same-level copies and all restrictions. Each reads only interior cells of other blocks and writes only ghosts, so the whole phase is race free and order independent.\nphase2 holds the prolongations, grouped by target level and ordered coarsest target first. The sweep is required because a prolongation stencil may read the coarse source's own ghosts, which may themselves have been prolongated from a still-coarser block — legal under 2:1 balance, where levels l-2, l-1, l can meet.\nboundaries lists the ghost regions facing outside a non-periodic domain, filled by the user hook after the inter-block phases.\n\nPeriodic boundaries appear nowhere special here: the tree wraps around, so they are ordinary copies, restrictions, and prolongations.\n\nA schedule is tied to the forest's leaf array as it was when built. It must be rebuilt after any refinement, coarsening, or regridding.\n\nGhostSchedule(forest, operators::Operators; T=Float64)\n\noperators is required: interpolation order follows from the application's discretization, so there is no order the mesh could sensibly default to. See Operators.\n\n\n\n\n\n","title":"TreeAMR.GhostSchedule"},{"category":"function","location":"#TreeAMR.isstale","page":"Home","text":"isstale(schedule::GhostSchedule)\n\nWhether the forest has changed since schedule was built, in which case it must be rebuilt before fill_ghosts! will accept it.\n\n\n\n\n\n","title":"TreeAMR.isstale"},{"category":"function","location":"#TreeAMR.fill_ghosts!","page":"Home","text":"fill_ghosts!(fs::FieldSet, schedule::GhostSchedule; boundary=nothing)\n\nFill every ghost cell of every block, by replaying schedule.\n\nThe three cases — same-level copy, restriction from finer neighbors, prolongation from a coarser neighbor — run in the phases described in GhostSchedule: copies and restrictions together first, then prolongations swept coarsest target first, then the physical boundary hook. Each phase is an embarrassingly parallel loop over blocks with a barrier between phases.\n\nPeriodic boundaries need nothing special; the tree wraps around, so they are ordinary transfers.\n\nboundary is called once per ghost region facing outside a non-periodic domain, as\n\nboundary(fs, blockindex, key, δ, region)\n\nwith key the block's MortonKey, δ the outward direction, and region the CartesianIndices of the ghost cells in stored coordinates. Use cell_center to get their positions. Passing nothing leaves those ghosts untouched, which is what a fully periodic domain wants.\n\nnote: Where the boundary hook runs\nThe hook runs after the copies and restrictions but before the prolongation sweep, not after every inter-block phase. A block sitting against the domain edge has prolongation stencils that reach tangentially past that edge, into the coarse source's own outer ghosts; filling those last would feed unwritten memory into the interpolation. The hook may therefore read the block's interior (as reflecting and extrapolating conditions do) but not other blocks' ghosts.\n\n\n\n\n\n","title":"TreeAMR.fill_ghosts!"},{"category":"function","location":"#TreeAMR.boundary_by_coordinates","page":"Home","text":"boundary_by_coordinates(f)\n\nA boundary hook that sets each outer ghost cell from f(x, v), with x the cell center and v the variable index — the same signature fill_by_coordinates! takes.\n\nUseful when the exact solution is known (manufactured solutions, convergence tests); real applications supply their own hook to impose outgoing, reflecting, or symmetry conditions.\n\n\n\n\n\n","title":"TreeAMR.boundary_by_coordinates"},{"category":"function","location":"#TreeAMR.statelength","page":"Home","text":"statelength(fs::FieldSet)\n\nNumber of entries in this field set's state vector: N^D · nvars · nblocks, counting interior cells only.\n\n\n\n\n\n","title":"TreeAMR.statelength"},{"category":"function","location":"#TreeAMR.statevector","page":"Home","text":"statevector(fs::FieldSet)\n\nA freshly allocated, zeroed state vector for fs — the u an ODE integrator advances. Use gather! to load the field set's current interior values into it.\n\n\n\n\n\n","title":"TreeAMR.statevector"},{"category":"function","location":"#TreeAMR.statearray","page":"Home","text":"statearray(u, fs::FieldSet)\n\nView a flat state vector as an (N, ..., N, nvars, nblocks) array, sharing memory. This is the layout application kernels write du in; the index order matches the working array, minus the ghosts.\n\n\n\n\n\n","title":"TreeAMR.statearray"},{"category":"function","location":"#TreeAMR.scatter!","page":"Home","text":"scatter!(fs::FieldSet, u::AbstractVector)\n\nCopy a state vector into the working array's interior cells, leaving ghosts untouched. The first step of every RHS evaluation; follow it with fill_ghosts!.\n\n\n\n\n\n","title":"TreeAMR.scatter!"},{"category":"function","location":"#TreeAMR.gather!","page":"Home","text":"gather!(u::AbstractVector, fs::FieldSet)\n\nCopy the working array's interior cells into a state vector — the inverse of scatter!.\n\nAn application's RHS kernels normally write du in state layout directly, fusing this away; gather! is for setting up initial data and for reading results back out.\n\n\n\n\n\n","title":"TreeAMR.gather!"},{"category":"function","location":"#TreeAMR.map_blocks!","page":"Home","text":"map_blocks!(kernel!, fs::FieldSet, args...)\n\nLaunch a KernelAbstractions kernel over every interior cell of every block, with ndrange = (N, ..., N, nblocks). The kernel's global index is therefore (i1, ..., iD, b) with each i running over 1:N; add G to reach the working array's stored indices.\n\nBlocks are uniform work units, so this is one flat parallel loop — the same launch that will be threaded in M5 and run on a device in M6.\n\n@kernel function rhs!(du, @Const(work), @Const(h), ::Val{D}, ::Val{G}) where {D,G}\n    I = @index(Global, NTuple)\n    b = I[D + 1]\n    c = ntuple(d -> I[d] + G, Val(D))          # stored (ghosted) index\n    du[ntuple(d -> I[d], Val(D))..., 1, b] = work[c..., 2, b]\nend\n\n\n\n\n\n","title":"TreeAMR.map_blocks!"},{"category":"function","location":"#TreeAMR.volume_weighted_norm","page":"Home","text":"volume_weighted_norm(fs::FieldSet, u::AbstractVector; p=2)\n\nThe p-norm of a state vector with each cell weighted by its volume, normalized by the domain volume — so the result is a resolution independent RMS (or, for p = Inf, the plain maximum).\n\nWeighting matters on an adaptive mesh: refined regions contribute more entries per unit volume simply for being refined, so an unweighted norm silently emphasizes them. This is also the shape an adaptive integrator's internalnorm needs; through M3 only fixed-dt integrators are exercised, so it is used here for error measurement.\n\n\n\n\n\n","title":"TreeAMR.volume_weighted_norm"},{"category":"type","location":"#TreeAMR.RegridFlag","page":"Home","text":"RegridFlag\n\nWhat the application wants done with a block: Refine, Coarsen, or Keep it as is.\n\nFlags are requests, not commands. Refinement is completed outward to maintain 2:1 balance, and coarsening happens only where all 2^D siblings ask for it and balance still permits — see complete_marks.\n\n\n\n\n\n","title":"TreeAMR.RegridFlag"},{"category":"function","location":"#TreeAMR.flag_blocks","page":"Home","text":"flag_blocks(f, forest) -> Vector{RegridFlag}\n\nBuild a flag vector by calling f(b, key) for every leaf, with b the block index and key its MortonKey.\n\nA convenience for host-side flagging; an application is free to produce the vector any other way, which is what will let the flagging kernel run on the device in M6 while the completion logic stays on the host.\n\n\n\n\n\n","title":"TreeAMR.flag_blocks"},{"category":"function","location":"#TreeAMR.complete_marks","page":"Home","text":"complete_marks(forest, flags) -> Vector{MortonKey}\n\nThe sorted leaf array that flags asks for, completed so that the result is still 2:1 balanced.\n\nRefinement is applied first, then coarsening — but only for sibling groups where all 2^D children are present as leaves and all of them ask to coarsen, since refinement is all-or-nothing. The result is then balanced, which may refine blocks the application did not flag, and may undo a coarsening that balance cannot support.\n\nforest is not modified.\n\n\n\n\n\n","title":"TreeAMR.complete_marks"},{"category":"function","location":"#TreeAMR.regrid!","page":"Home","text":"regrid!(forest, fieldsets, schedule; flags, boundary=nothing, transfer=true)\n\nRefine and coarsen forest as flags asks, and move every field set's data onto the new mesh. Returns true if the mesh changed.\n\nThe steps are those in CODE.md: complete the marks to preserve 2:1 balance (complete_marks), build the new sorted key list, allocate fresh block storage, and transfer — surviving blocks copied, newly refined blocks prolongated from their parent, coarsened blocks restricted from their children.\n\nfieldsets is a single FieldSet or a collection of them, all over forest. Each one's storage is replaced in place, so references an application already holds stay valid, but block indices do not survive: slots are compacted, and forest.leaves[b] is the only way to say which block is which.\n\nschedule must be the current schedule for forest. Ghosts are filled from it before the transfer, because a prolongation stencil reads its parent's ghost layers; pass boundary if the domain has non-periodic faces. Afterwards the schedule is stale and the state vector has changed length, so an application must rebuild both:\n\nif regrid!(forest, fs, schedule; flags = flags)\n    schedule = GhostSchedule(forest, operators)\n    u = statevector(fs); gather!(u, fs)      # then reinit! the integrator\nend\n\nSet transfer = false to rebuild the mesh and storage without moving data — what the initial-data cycle wants, since it re-evaluates the initial data on the new mesh instead (see adapt_to_initial_data!).\n\n\n\n\n\n","title":"TreeAMR.regrid!"},{"category":"function","location":"#TreeAMR.adapt_to_initial_data!","page":"Home","text":"adapt_to_initial_data!(fs, operators; initial, flag, maxpasses=10, boundary=nothing)\n\nRun the initialization cycle from CODE.md: fill the initial data, flag, regrid, then re-evaluate the initial data on the new mesh rather than interpolating it, and repeat until the hierarchy stops changing.\n\nRe-evaluating is the point: interpolating initial data onto a newly refined block would bake in the coarse mesh's resolution, so the refinement would never buy anything.\n\ninitial is an (x, v) -> value callback as fill_by_coordinates! takes, and flag is (b, key) -> RegridFlag as flag_blocks takes. Returns (schedule, passes, converged); converged is false if the hierarchy was still changing when maxpasses ran out.\n\n\n\n\n\n","title":"TreeAMR.adapt_to_initial_data!"},{"category":"function","location":"#TreeAMR.total_mass","page":"Home","text":"total_mass(fs::FieldSet, var=1)\n\nThe volume integral of one variable over the domain — Σ hᴰ u over every interior cell, with each block weighted by its own cell volume.\n\nWhat regrid! does to this depends on the operator family:\n\nWith Conservative operators it is preserved to roundoff for any field. Restriction is the exact volume average, and prolongation reconstructs over the coarse cell preserving its average, so a parent's children always average back to it.\nWith PointValue operators it is preserved only for fields the operators reproduce exactly. Coarsening alone still conserves any field — order-2 restriction is the 2^D average — but refinement does not: prolongation is not locally conservative, and at a refinement boundary the fine region draws on neighbor values through the parent's ghosts without those neighbors giving anything up.\n\n\n\n\n\n","title":"TreeAMR.total_mass"},{"category":"type","location":"#TreeAMR.Stencil1D","page":"Home","text":"Stencil1D{T}\n\nOne dimension of a transfer: for each cell of the target's index range, the first source cell and the order weights to apply from there.\n\nsrcstart[i] and weights[:, i] describe target cell targetfirst + i - 1.\n\n\n\n\n\n","title":"TreeAMR.Stencil1D"},{"category":"type","location":"#TreeAMR.TransferGroup","page":"Home","text":"TransferGroup{T,D}\n\nAll transfers that share one set of 1D stencils — same kind, same direction, same child offset — reduced to a list of (targetblock, sourceblock) index pairs. One kernel launch per group.\n\n\n\n\n\n","title":"TreeAMR.TransferGroup"},{"category":"type","location":"#TreeAMR.BoundaryRegion","page":"Home","text":"BoundaryRegion{D}\n\nA ghost region of a block that faces outside a non-periodic domain, and so is filled by the user's boundary hook rather than by an inter-block transfer.\n\n\n\n\n\n","title":"TreeAMR.BoundaryRegion"},{"category":"function","location":"#TreeAMR.lagrange_weights","page":"Home","text":"lagrange_weights(nodes, x)\n\nWeights w with sum(w[i] * u(nodes[i])) == u(x) for every polynomial u of degree less than length(nodes).\n\nExactness holds for any distinct nodes, which is what lets the restriction stencil be shifted away from the coarse-fine interface (to stay inside the fine block's interior) without losing order.\n\n\n\n\n\n","title":"TreeAMR.lagrange_weights"},{"category":"function","location":"#TreeAMR.ghost_layers_read","page":"Home","text":"ghost_layers_read(ops::Operators)\n\nHow many of the coarse source block's own ghost layers a prolongation stencil reaches into — the quantity that ties the operator order to G.\n\n\n\n\n\n","title":"TreeAMR.ghost_layers_read"}]
}
